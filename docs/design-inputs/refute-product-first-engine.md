# Refutation: proposal-product-first, through the engine and contracts lens

Scope: every Cypher statement against `00`; the ingest against `04 section 1`; the five reads against
`04 section 2` and `02 section 4`; the plugin contract; the test plan against `04 section 5`; the
migration steps. Code checked directly where a contract turned on it.

Verdicts: **HOLDS** (the design already satisfies it), **FIXABLE** (wrong or missing, repairable
inside the design), **FATAL** (cannot be satisfied without changing a stated principle).

---

## (a) Every Cypher statement against `00`

| # | item | verdict | evidence | what would fix it |
| --- | --- | --- | --- | --- |
| A1 | `6.1` page query does not compile | FIXABLE | `WHERE ($uris IS NULL OR EXISTS { MATCH (:Media)-[:MEMBER_OF]->(c) WHERE uri IN $uris })`: the Media node is anonymous, so `uri` is not a bound variable. `00`: "`conn.query` THROWS on a binder error (it does not return isSuccess false)", so this is a thrown exception on the app's most-run read, not a wrong answer | bind the node (`MATCH (m:Media)-[:MEMBER_OF]->(c) WHERE m.uri IN $uris`); and split the `$uris IS NULL` branch into two queries rather than passing a null list param into a strictly typed binder (`00`: "Params are strictly typed") |
| A2 | `6.1` groups an aggregation by a NODE and by a LIST | FIXABLE | `WITH c, collect(m {...}) AS members` then `WITH c, members, a, k, t, collect(...)` then `RETURN c, members, collect(...)`. The second and third aggregations group by `c` (a node) and `members` (a list of structs). `00` verified `collect(DISTINCT ...)` and records `Order by NODE is not supported`, i.e. the engine already refuses one node-valued operation; group-by on a node and on a list is not in the verified set, and the repeated list is re-hashed per container row | collect containers into their own `WITH` before the member collect, or key both aggregations on `c.id` (a STRING) and re-join in JS. Measure before relying on it |
| A3 | map projection `m {.uri, .origin, ...}` is used in four load-bearing queries and is not in `00` | FIXABLE | `6.1`, `6.4`, `P0`'s scan (`c {.kind, .targetScope, ...}` on a REL), `P4`'s scan. `00`'s verified list has `to_json({...})`, `collect(DISTINCT ...)` and `json_extract`, and no projection syntax at all. `00` is explicitly "measured, not documented", so absence is not support | one measurement in step 1; the fallback is naming every field in the collect, which is semantically identical and only verbose |
| A4 | filtered variable length paths are written without the bound relationship variable `00` verified | FIXABLE | proposal `3.5`: `-[:LINK*0..8 (r, _ \| WHERE r.kind = 'SAME_AS' ...)]-`. `00`'s verified form is `-[e:CLAIMS*1..3 (r, _ \| WHERE r.relation = 'INCLUDES')]->`, with a name on the pattern | name the pattern, or measure the unnamed form. Same for the two copies in `7.5` |
| A5 | the `7.5` "why is this uri in this cluster" query stacks three unverified features | FIXABLE | `MATCH p = (...)-[:LINK*1..8 (r, _ \| WHERE ...)]-(...) RETURN [r IN rels(p) \| {by: r.by, ...}] LIMIT 1`: a named path over a FILTERED recursion, `rels(p)`, a list comprehension building struct literals, and `r` rebound inside the comprehension while the filter also binds `r`. None of the four is in `00` | build the trace from the materialized `LINK` rows with `supports` instead of from a path, which is what `supports` exists for |
| A6 | the anomaly trace query is always true | FIXABLE | `7.5`: `MATCH (c:Cluster) WHERE size(c.anomalies) > 0`. `Cluster.anomalies` is declared `JSON` (`2.2`), and `00` records that JSON "reads back as a string", so `size()` is the STRING length and `'[]'` has length 2. "Is the graph contradicting itself?" answers every cluster, forever. This is the check-the-checker failure in the one query whose whole job is to report a contradiction | store `anomalyCount INT64` beside the JSON and test that, and pair the test with a control cluster that must report zero |
| A7 | the generic writer cannot write the two DATE columns | FIXABLE | `MediaProfile.startDay DATE` and `EpisodeProfile.day DATE` (`2.2`), written by a writer that takes `rows: Record<string, unknown>[]` (`5.1` `PluginOutput`) and applies them with `UNWIND`. `00`: "a STRING param into a DATE column is a binder error; write `date($x)` in the query". Dates originate as strings inside `raw`, so every profile write throws | a per-table column-type map in the writer that emits `date(r.startDay)`; or make the profile columns STRING and cast at the two places that compare them (`P2`'s span scan compares `min(pe.day) <= pr.startDay`) |
| A8 | `MERGE` on a relationship pattern with properties is unverified, and `MERGE` being all-or-nothing pins `CLAIMS.node` to the first answer | FIXABLE | `4.2` step 5: `MERGE (a)-[c:CLAIMS {kind, claimer, provenance}]->(b) ON CREATE SET c.node = h.node, ...`. `00` verified `MERGE` on a NODE pattern only, and that "MERGE is all or nothing over its pattern". The proposal's own `11.1` admits the first half. The second half is not noticed: a claimer that answers twice about one pair (the listing row then the modal row, `04 section 1.3`, `02 section 2.5`) keeps its FIRST, poorest `node`, and `6.3` draws the container badge's url from "the best-scored claimer's `node.url`", so the badge can keep a truncated description forever (the `titles { title }` class of `01 I5.3`) | `ON MATCH SET` with the same field-merge rule used for `Media.raw`, keyed on `answerSeq` |
| A9 | `P2`'s span scan is a cartesian product, and it runs every pass | FIXABLE | `5.4 P2`: `MATCH (c:Cluster {scope:'RUN'})<-[:MEMBER_OF]-(r:Media)<-[:PROFILE_OF]-(pr)` then, with no join predicate to `r`, `MATCH (s:Media)<-[:PROFILE_OF]-(ps:MediaProfile {folding: true}) WHERE ps.count > c.runLength AND NOT EXISTS {...}` then expanded over `(s)-[:HAS_EPISODE]->(:Episode)`. Every run member crossed with every folding-origin row crossed with that row's episodes. `00`'s 35 ms plugin scan was a JOINED `NOT EXISTS` over one origin pair on 5,796 media; this one has no join at all, and `11.1` claims "every plugin is delta-scoped" while this query carries no scope | anchor the candidate set on an existing edge (`MATCH (r)-[:LINK {kind:'PART_OF'}]->(s)` or `ATTACHED_TO`) or on a shared `year`, and restrict `c` to `delta.clusters` |
| A10 | `P0` scans the append-only `Answer` log on a non-key column, while the join edge for it is declared and unused | FIXABLE | `5.4 P0`: `OPTIONAL MATCH (a:Answer {uri: u})`. `Answer`'s primary key is `seq` (`2.1`), so `uri` has no index; the log is append only, one row per resolver return, including every re-resolve driven by the fan-out and the re-asks. `ABOUT(FROM Answer TO Media, ...)` exists in the schema (`2.1`) and no query uses it | walk `(m)<-[:ABOUT]-(a:Answer)` instead; or keep the CONTAINER evidence P0 actually needs (`scopesSaid`) as a column on `Media` maintained by the ingest, which already reads the row |
| A11 | the read budget is met in QUERY COUNT and missed in BYTES | FIXABLE | `G5` and `6.1` count one query per page, which matches `00`'s "a 60 cluster page with containers and episode counts in ONE query: 49 ms". But `6.1` returns `m {.raw}` for every member of every cluster, and `00` records JSON reads back as a STRING, so `aggregateFields` must `JSON.parse` every member on every render. `2.1` projects only `score, episodeCount, startDate, endDate, type, status, season, seasonYear, titles, categories`; `covers`, `banners`, `descriptions`, `shortDescriptions`, `genres`, `tags`, `trailers`, `nextAiringEpisode`, `popularity`, `franchise` and `relations` are drawn by the card and the modal (`04 section 2.3`) and exist only inside `raw`. `00`'s 49 ms figure did not carry `raw` | project the fields the reads actually use (they are a closed set: `04 section 2.3`), and keep `raw` for the trace path and the replay only |
| A12 | the engine is one single-threaded lane and the proposal never sums a 50 ms window | FIXABLE | `00`: the DEFAULT variant is "single threaded, async: the engine runs in its own Web Worker", every call "an async round trip ... about 1 to 2 ms floor". Into that one lane the design puts: the ingest commit (6 statements per batch), up to four iterations of six plugins per flush, every `Subscription.media` and `mediaPage` re-read, and `ctx.findAggregatedMedia` from seven blocking extractors (`04 section 1.8`, `src/sources/utils.ts:488-508`). Every number quoted in section 11 (`35 ms`, `1.7 ms`, `107 ms`) is a single operation on an idle engine | a cost model for one 50 ms window at 24 sources landing, measured in step 1 with the tee running; and move `title` and `range` off the per-flush schedule onto an idle or debounced schedule |

---

## (b) The ingest against `04 section 1`

| # | item | verdict | evidence | what would fix it |
| --- | --- | --- | --- | --- |
| B1 | `useOnResolve` keyed on the named type, its three consequences, and the fan-out that discards payloads | HOLDS | `4.1` keeps all of it and names the consequences (fires on the return value not the selection; episodes only when `episodes` resolves; the media inserter never writes `media.episodes`), matching `04 section 1.2`. The graph stays the only join point (`04 section 1.5`) | |
| B2 | the "250 rows is well under 50 ms" argument reads its own number backwards | FIXABLE | `4.2` step 4: "2,000 nodes in 107 ms batched (00), so a batch of 250 is well under the 50 ms schedule". The DataLoader's `maxBatchSize: 250` counts TOP-LEVEL medias, and `mediaInserter` flatMaps each through `recursivelyUnwrapMediaHandles` first (`src/worker/extractor.ts:107`), so one batch is routinely thousands of rows: 2,000 is the realistic batch, not the reassuring ceiling. The commit is also six statements (answers, read-back, merge, claims, relations, episodes), not one, at a 1 to 2 ms floor each. (The favourable half is real and worth recording: the three loaders are MODULE level, `src/worker/extractor.ts:106,136,154`, so all 24 sources share ONE batch per 50 ms, not 24) | budget the unwrapped row count, not the loader's batch size; measure a real `MEDIA_PAGE` flush in step 1 |
| B3 | the ingest cannot compute the `changed` set it emits | FIXABLE | `4.2` step 7 emits `changed = {media: [...], claims: [new edge keys]}`, but step 2 reads only `Media` (`UNWIND $uris AS u MATCH (m:Media {uri: u}) RETURN m.uri, m.owned, m.raw, m.fieldSeq`) and `MERGE` reports no newness. Today `graph.edge`'s NEW return is what stopped "a source re-minting the same handle emitted `media:changed` forever" (`graph.ts:293-296`), and the ASSERTED writes' returns are discarded for the same reason (`db.ts:173-176`) | read the existing claim keys in step 2 alongside the rows. The design's second line of defence is sound and worth keeping either way (see B4) |
| B4 | replacing a boolean newness test with "run six plugins and diff" | FIXABLE | `6.6`: "An idempotent batch produces no pass output and therefore no event". That is correct for the EVENT contract (`04 section 3`, `edge-idempotence.test.ts`), and it is a better guarantee than today's. The cost is that the guard moved: today a re-asserted handle costs one boolean; here it costs a full pass (A9's cartesian scan, `P3`'s pairwise wasm loop, `P5`'s whole-graph union-find) on every flush, and `04 section 3` records that the sources "re-mint constantly because the DataLoader flushes on a 50 ms timer and the media page re-asks every origin" | fix B3 so a no-op batch emits no `graph:changed` at all, and let the pass be the backstop rather than the gate |
| B5 | a re-fetch of the same uri, and episodes arriving before their media | HOLDS | `4.3` keeps `lastWriteLongestArray`'s rule verbatim (`02 section 2.5`, restated as `04 section 5.1` demands), archives the loser in `Answer`, and records `fieldSeq`. Episodes `MERGE` their media as a placeholder (`4.2`), which is strictly better than today's `pendingClaims` deferral (`02 section 2.4`) | |
| B6 | `pendingClaims` replaced by a query and by recomputation | HOLDS | `4.3`: the placeholder IS stored, the claim edge is written at once, and `plugin:direct` simply does not evaluate it until both endpoints have an effective scope. "The pending set is the query `MATCH (m:Media {owned: false})`, with no expiry map to leak" answers `02 section 2.4`'s recorded lack of expiry and `01 case 48` at the same time. Arrival order genuinely stops deciding, because every pass recomputes from the same evidence | |
| B7 | `Answer.selection` claims more than the hook can see | FIXABLE (minor) | `2.1`: `selection STRING[]` is "the field names the resolver actually returned". `useOnResolve` sees the RESULT object (`src/worker/extractor.ts:471-497`), so a field resolved to null and a field not selected are indistinguishable. Only the trace reads it, so the cost is a misleading trace line | take the selection from `info` in the hook, where the selection set actually is |

---

## (c) The read contracts (`04 section 2`, `02 section 4`, the event contract)

| # | item | verdict | evidence | what would fix it |
| --- | --- | --- | --- | --- |
| C1 | the promised container badge for a placeholder cannot be drawn | FIXABLE | `2.1` states the point of `CLAIMS.node`: "a container badge can carry the url Kitsu published for a Crunchyroll series without Crunchyroll ever answering (01 case 1)". But `P5` says "a placeholder is a member only if an active `SAME_AS` reaches it", a PART_OF-only container is reached by no SAME_AS, so it is in no `Cluster`; `P2`'s attachment scan requires `(t:Media)-[:MEMBER_OF]->(k:Cluster)`; and `6.1` reads containers only through `(c)-[a:ATTACHED_TO]->(k:Cluster)<-[:MEMBER_OF]-(t:Media)`. The badge of `6.5` row 1 and the fallback of `6.3` ("for a placeholder, the best-scored claimer's `node.url`") are unreachable. Walkthrough `8.1` assumes the opposite and speaks of "the `cr:G24H1N3MP` container cluster" | give every row with an effective scope a singleton `Cluster` (placeholders included), or read containers from `LINK {kind:'PART_OF'}` directly and treat `ATTACHED_TO` as an optimisation |
| C2 | I14 is claimed to hold and does not: a CONTAINER cluster has slots | FIXABLE | `11.3 I14`: "P5 groups inside one cluster; the slot read cannot be handed a container". `P5` mints a `Slot` for any cluster whose members hang `HAS_EPISODE` from their own origin, and `6.5` row 5 deliberately draws a container's own episodes. `db.ts:465-468` states the precondition this breaks: `mergeByEpisodeNumber` is "SAFE ONLY BECAUSE THE INPUT IS ONE RUN ... this must never be handed a container's uris". The origins that hurt are the ones `04 section 5.3` names: `paramount:<slug>`, `trakt:<slug>`, `tvdb:<seriesId>` flatten every season, so slot `#1` holds one episode per season on that container's page | gate slot minting on `Cluster.scope = 'RUN'`, and give a container page its `preferredRun`'s slots or none |
| C3 | `exportStore` over raw `CLAIMS` re-admits the welds the export exists to exclude | FIXABLE | `9.1`: "the walk becomes a query over `CLAIMS` with `provenance IN ['source','ask']`". Today the walk is over `ASSERTED_LABELS.RUN` (`export.ts:54`), which is written ONLY for a claim whose two scopes are equal and whose kind was SAME_AS (`db.ts:186-196`, read in full). A raw CLAIMS walk includes every cross-scope SAME_AS claim (kitsu into `cr:G24H1N3MP`, simkl and tvmaze into `imdb:tt13303712`), which is exactly the three-seasons-in-one-component weld of `01 case 1`, and the seed is built from this output (`offline/seed-build.test.ts`, gated by `seed-gate.test.ts`) | walk `LINK {kind:'SAME_AS', status:'active', by:'plugin:direct'}` whose `supports` name a `source` or `ask` claim. That is the exact successor of the asserted adjacency, and it also restores the CONTAINER and PART_OF separation the three asserted labels carry |
| C4 | `view:changed` no longer wakes a source waiting on a field that arrives inside an existing row | FIXABLE | `6.6`: it fires "only when a cluster's membership, slots, attachments, run length or anomalies moved". An owner's richer second answer about an existing member changes `raw` and nothing structural, so no event fires. Today `episode:changed` fires UNCONDITIONALLY (`db.ts:414`) and wakes every `ctx.listenForMediaChanges` consumer, and `04 section 3` states the requirement plainly: "A replacement that fixes this MUST check that `waitForMedia`'s seven callers still get woken (`src/sources/utils.ts:500`)". The proposal fixes it and does not do the check; the seven callers are unogs, appletv, tmdb, crunchyroll, justwatch and tvmaze, each with a 15 s or 30 s deadline | emit `view:changed` for a cluster whose members' `raw` changed as well, or give `ctx.listenForMediaChanges` its own `row:changed { uris }` fed by the ingest's `changed` set |
| C5 | `Media._id` is no longer monotone, and `Episode._id` inherits every cluster id change | FIXABLE | `04 contract 1` and `src/urql-keys.ts:17,38`: `_id` is the graphcache key and must be stable as a cluster grows, or every card's DOM node is replaced. `5.3`: "when a plugin is enabled, disabled, or bumps `version`, the writer retracts everything stamped with its id", and `plugin:aggregate` owns `Cluster.id`, so any version bump of the aggregate re-mints every id in the session. `P5` also admits splits ("when a cluster splits ... the other part is new"), which the union-find could never do. `Slot.id = '<clusterId>#<number>'` (`2.2`) makes every episode key inherit both, plus every renumbering by `plugin:range`. `11.1` budgets only "the first `_id` change breaks the urql cache once" | derive `Cluster.id` from something retraction cannot move (the lowest member uri at first sight, kept in a table `plugin:aggregate` may not retract), and key `Slot` on that plus the number |
| C6 | `Media.uri` (`ag:(...)`) can now SHRINK, and it is a shared, pasteable address | FIXABLE | `04 contract 2`: it "is a shareable route, and only grows" (`src/utils/uri.ts:143-149` `shouldGrowAddress`, `src/party/protocol.ts:28` syncs a PATH between viewers). `P5` claims it "grows monotonically while the closure grows", but retraction is a first-class feature of this design (`5.3`, `11.3 I1`), so a refused or version-bumped link shrinks it. Nothing in the proposal says what a live subscription, a bookmark or a party path does then | state the rule: the aggregated uri is append only per cluster id, and a member that leaves stays in the address as an alias the resolve query still accepts (`6.2`'s `EXISTS { m.uri IN $uris }` already does the right thing) |
| C7 | `askUnasked`'s termination argument is re-keyed onto the one thing that can now move | FIXABLE | `src/worker/resolvers/media/index.ts:55-56`, read directly: "an origin enters the set once and never leaves, and the cluster only ever gains members (union-find unions, it never splits), so each source is asked at most twice". `7.2` re-keys it on "(cluster id, origin, uri)", and cluster ids now merge and split | keep the key as it is in the code, a per-subscription Set of ORIGINS. It survives splits untouched, and `7.2`'s claim then becomes true rather than asserted |
| C8 | the whole-store fallback, the filter order, strictness, search and sorts | HOLDS | `6.1` keeps the no-uris seed (`04 section 2.1`'s "load bearing", measured 2026-09-06), runs the hide inside the query and `applyMediaFilters` after it (`04 section 2.1`'s ordering requirement, `media/index.ts:133-136`), and keeps the filter verbatim including its strictness (`02 section 4.7`) | |
| C9 | the store-wide hide of attached containers | HOLDS | `6.1` hides a container when ANY run cluster attaches, where `db.ts:391-398` hides it when a run cluster is IN THE SAME LIST. On the whole-store fallback the two coincide; on the `insertedUris` path store-wide is the more consistent of the two (today the same show's container card appears on the warm read and not on the cold one). `11.1` already asks for the fixture | |
| C10 | the singleton branch of `aggregateMedia` is removed, which changes every single-source route | FIXABLE (minor) | `P5`: "the singleton branch of `aggregateMedia` that skipped every normalization (02 section 4.1) is gone". That is the right call, and `02 section 4.1` records the singleton path as a different contract. The consequence is unlisted: a one-member card's `uri` goes from `mal:39535` to `ag:(mal:39535)`, its `origin` from `mal` to `ag`, its `url` from the source's url to the app route. Every existing bookmark and every party path of a single-source page changes shape | name it in the migration and in the `uri-aggregated` rewrite; `asAggregatedUri` already exists (`src/utils/uri.ts:107-128`) so the router side is a one-line normalisation |
| C11 | `INCLUDES` added to the GraphQL `MediaHandleRelation` enum | FIXABLE (minor) | `6.3` adds an output-only third value. The badge reader takes "SAME_AS first then anything" and playback takes SAME_AS only (`04 section 2.3`), so the UI is safe, but a new enum member is a client-visible schema change and `src/urql-keys.ts:22-30` keys `MediaHandle` to null, so it embeds into whatever holds it | flag it in the schema review; add a case to `episode-origins.test.ts` proving an `INCLUDES` handle never yields a play button |

---

## (d) The plugin contract

| # | item | verdict | evidence | what would fix it |
| --- | --- | --- | --- | --- |
| D1 | **whole-output diff and delta scoping cannot both hold** | **FATAL** | `5.1`: `run` must "Compute the plugin's WHOLE desired output from the graph", and the writer "diffs it against what this plugin wrote last time (by = id) and applies the delta", with "A retract is an empty `desired`" (`5.2`). `5.1` also hands the plugin `delta` "so a scan can be scoped", and `5.4` uses it: `P1`'s scan is "scoped to `delta.claims` on an incremental pass", `P3`'s is "scoped to clusters with a changed profile on an incremental pass". A scoped run returns a PARTIAL desired set, so the writer deletes every `LINK` that plugin wrote for anything outside the delta. On the second flush of any session, `plugin:title` and `plugin:range` retract nearly all of their own output, then re-mint it on a later full pass, and the graph oscillates every 50 ms. Both repairs cost a principle: recompute fully every pass and the cost budget of `11.1` cannot hold (`P3`'s pairwise wasm loop is today's whole fuzzy pass, `02 section 2.10`, now run up to four times per flush instead of once per 100 ms debounce); or scope the diff and "idempotent by construction", which is what `5.3`, `9.3` and `11.3 I1` all rest on, stops being a property of the contract and becomes a property each plugin has to be trusted to have | make the scope explicit and part of the output: `run` returns `{scope: <the keys it recomputed>, output}` and the writer diffs ONLY within that scope, with a rule for an edge whose endpoints straddle the boundary. Then state idempotency as "same graph, same scope, same output", and have `runPlugin` assert it that way |
| D2 | the core cannot prove a plugin left a source row alone | FIXABLE | `5.3` offers three proofs. (1) is a TypeScript type, erased at runtime, plus validation of the rows a plugin RETURNS, which is not where an accidental write would come from. (2) is a string checker for `CREATE, MERGE, SET, DELETE, DROP, COPY, ALTER` "outside string literals": `00` records that multi statement `query("...; ...")` works, the list omits `CALL`, `ATTACH`, `LOAD`, `IMPORT`, `EXPORT`, `DETACH` and `REMOVE`, and nothing is said about comments, so the checker is a parser problem solved with a word list. (3) is the only runtime proof and it is `max(m.seq), count(m)` per table: a `SET m.scope = 'RUN'` on an existing row moves neither, and `scope` is precisely the column a profile plugin would be tempted to write | run `ctx.query` on a connection opened read only if the engine offers one; otherwise hash the source tables in production the way `9.3` already hashes them in the harness, and compare per pass. The hash is the proof; the other two are ergonomics |
| D3 | two plugins disagreeing is only guarded in one direction, and only at proposal time | FIXABLE | Guard 6 refuses a `SAME_AS` when an active `PART_OF` or `INCLUDES` already joins the pair (`3.5`, `5.2`). Nothing refuses the reverse: `partOf`'s guards are only `unknown-scope`, `self`, `inverted` (`5.2`). So `plugin:title` welds a pair in pass N, a later kitsu `asserted` claim arrives and `plugin:direct` writes `PART_OF` over the same pair in pass N+1, both are active, and the `SAME_AS`-only closure of `3.5` merges them. The owner's rule is then broken by arrival order, which is the one thing `G3` promises cannot happen | evaluate the containment rule as a post-pass invariant over the whole graph (it is one query) and retract the weaker edge by a declared precedence, not by which plugin ran first |
| D4 | a plugin that throws mid-run is unspecified | FIXABLE | Nothing in `5.1`, `5.2` or `5.3` says what happens. The writer has already applied earlier plugins' deltas, so the graph is half updated; if the pass aborts, `view:changed` does not fire, and `04 section 3` states the consequence: "An event that does not fire leaves a page empty forever" | per-plugin try/catch: a throwing plugin keeps its PREVIOUS output (never an empty desired set, which would retract everything it ever wrote), the pass continues, `view:changed` still fires for what did run, and the failure is a `Cluster.anomalies` entry so it is visible in the same place as every other contradiction |
| D5 | ordering, cycles and the four-iteration cap | FIXABLE (minor) | `5.3`: "Cycles are legal; the pass iterates to a fixed point ... capped at four iterations per emit with a warning". A capped pass is by definition not a fixed point, and reads run against whatever state the cap left, so `G3` ("the same graph renders the same page") holds only between quiet periods. `11.1` argues the residue converges on the next flush, which is true and is not the same claim | qualify `G3`: the same graph renders the same page AT A FIXED POINT, and make the cap a logged anomaly rather than a warning, so a graph that keeps hitting it is visible |
| D6 | retraction and recompute are idempotent everywhere except the one place with state | FIXABLE | `5.3`'s full pass is genuinely reversible for `direct`, `title`, `range`, `containment` and `profile`. `plugin:aggregate` carries `Cluster.id` across passes through `ctx.previous`, and retracting it deletes exactly that state, so disable-and-re-enable is NOT identity for the ids the app caches on. Same defect as C5 | as C5: hold the id assignment in a table the aggregate may write but not retract |
| D7 | the guards are one chokepoint and the refusals are stored | HOLDS | `5.2` puts every `SAME_AS` from every source (handle, `similarMedia` answer, title match) through one guard list, which is what `01 case 2` and `01 case 7` ask for and what today's three pair linkers plus the scope dispatch plus the resolver comment cannot do (`02 section 3` items 4 and 5). Storing a refusal as `LINK {status:'refused', reason}` makes `01 case 6` a query instead of four silent short circuits | |
| D8 | episode identity per proven pair rather than per offset | HOLDS | `3.4` and `P4` rule 1 place a button only from an `EPISODE_LINK` carrying the two days, which directly refutes the offset vote of `01 R1` and gives `01 case 15` its piecewise mapping with no vote anywhere. `6.4`'s slot read reaches a play button only through `FILLS`, which exists only because a member hung it or a pair proved it | |

---

## (e) The test plan against `04 section 5`

| # | item | verdict | evidence | what would fix it |
| --- | --- | --- | --- | --- |
| E1 | `store/consensus.test.ts` is counted as KEEP verbatim and is a rewrite with real coverage loss | FIXABLE | `04 section 5.1` marks it KEEP (31 tests: "`tieredConsensus`, `runEpisodes` trimming a lent season, `alignmentOffset` by shared dates, `alignRunEpisodes`"). `9.2` lists it under "KEEP verbatim (55)" as the "pure half". But `9.1` deletes `alignmentOffset`, `alignRunEpisodes` and `runEpisodes` and replaces an offset with pairs, and an offset assertion has no successor: `consensus.test.ts:89-389` is three of the file's four subjects. So the 55/18/1 split is overstated by at least this file, and it is the file pinning the two defects the episode path exists for (The Elusive Samurai's 20 rows, `consensus.ts:93-97`; the lent season's 24 rows, `consensus.ts:222-224`) | move the file to REWRITE and restate each of its cases as a `plugin:range` or `P5` assertion, case for case, before the old functions are deleted in migration step 5 |
| E2 | the closure cross-check is bounded where the thing it checks is not | FIXABLE | `9.3`: `runPlugin` "asserts that the materialized closure equals the recursive one of section 3.5", and `3.5`'s query is `*0..8`. `P5`'s JS union-find is unbounded, so a component deeper than 8 hops makes the control report a disagreement that is not one, and `00` measured a dense-graph 1..10 hop recursion at 1,033 ms, so the control is slowest exactly on the shapes that need it | assert the union-find against an independent JS BFS over the same sorted edge list, and keep the Cypher recursion as a spot check on small fixtures with its cap stated |
| E3 | refusals asserted with a control | HOLDS | `9.3`: "A test that expects `refused: count-mismatch` also seeds the exact-count sibling and expects `active`, so a test that cannot express the failure reports nothing". That is the house check-the-checker rule applied correctly, and it is the strongest part of the plan | |
| E4 | the replay corpus | HOLDS | `9.3`'s `Answer` log replay ("the page rendered from the replay must equal the page recorded with them, byte for byte after sorting") is the only proposal mechanism that can pin a production bug as a test, and it is exactly what `02 section 5.2` says is impossible today ("No re-derivation from source answers ... A policy change cannot be replayed") | |
| E5 | `store/edge-idempotence.test.ts` rewritten to "no `view:changed` on a re-asserted claim" | FIXABLE (minor) | Correct assertion, but it no longer covers what the old one covered: that the WRITE path emits nothing. With B3 unresolved the write path can emit freely and the test still passes, because the pass absorbs it | keep both assertions: no `graph:changed` from an idempotent batch, and no `view:changed` from an idempotent pass |
| E6 | the 42 source tests and the 4 router tests | HOLDS | `04 section 5.3` records that only five source tests reach `src/worker/store/**` and two of those only in comments; `9.2` touches `offline/seed-source`, `offline/seed-build` and `offline/seed` exactly as `04` marks them | |

---

## (f) Migration

| # | item | verdict | evidence | what would fix it |
| --- | --- | --- | --- | --- |
| F1 | step 1 is not a day | FIXABLE | It contains: the engine plus `setWorkerPath` plus a 22.1 MB asset in `public/` (`00`); ten tables created at start including two DATE columns the writer cannot yet write (A7); the whole ingest of section 4, which is the answer log, the owner rule, the field merge, the claim walk that must reproduce `recursivelyUnwrapMediaHandles`'s PART_OF stop (`part-of-subtree.test.ts`), episodes, origins and the changed set (B3); and a tee beside three existing writers | split it: engine plus schema plus `Answer` log first (that genuinely is a day and is genuinely additive), the `Media`/`Episode`/`CLAIMS` ingest second |
| F2 | what step 1 says it ships cannot be shipped in step 1 | FIXABLE | "the bug-report-as-a-query story on production data" and "`?trace=1` runs the 7.5 queries against the shadow". Five of the eight `7.5` queries name `Cluster`, `Slot`, `FILLS`, `LINK` or `anomalies`, none of which is written until step 2. Step 1 ships an `Answer` dump and the three claim-level questions | say so, and move the trace panel to step 2 where its tables exist |
| F3 | "No read changes" is true of correctness and not of timing | FIXABLE | The tee doubles the write path inside the worker for every user (`11.2` decision 6 admits the shadow runs for everyone), adds six statements per 50 ms batch at a 1 to 2 ms floor each, and adds a 22.1 MB cold download. Today's `upsertMedia` is synchronous in-memory map writes (`db.ts:82`). The fuzzy pass already runs on the same thread inside `getPage` | measure the tee's cost on a real `MEDIA_PAGE` fan-out before it ships to everyone, and gate the engine load behind the flag until step 3 (which is `11.2` decision 6, stated as a decision and worth deciding this way) |
| F4 | step 3 runs two stores with two different merge policies at once | FIXABLE | Step 3 switches `Subscription.media` and `Media.episodes` to the shadow while `mediaPage` stays old, and `plugin:title` does not land until step 4. So the listing's clusters are fuzzy merged (the old `fuzzyMergeMediaClusters` inside `getPage`, `media/index.ts:127`) and the detail page's are direct only. A card links by the LISTING's `ag:(...)` uri; the new resolve query (`6.2`) finds the first RUN cluster holding ANY of those members, so the page opens with fewer badges and a different `_id` than the card that opened it, in the same session | move `plugin:title` into step 3, or keep `media` on the old store until `mediaPage` switches with it. Either way the two reads must never disagree about membership |
| F5 | steps 2, 4, 5, 6, 7 each leave the app running | HOLDS | Step 2 is shadow only with a diff report as its acceptance test, which is the right shape. Steps 4 and 5 are flag-gated swaps of one read at a time. Step 7 is extractor work that only adds evidence | |
| F6 | an undecided owner question sits on step 6's critical path | FIXABLE (minor) | Step 6 makes `buildHandlesFromUri` stamp `provenance: 'address'` and deletes the old store in the same step, and `11.2` decision 1 says the `address` rule "needs an owner decision" (it is `01` open problem 2, which the record says "is a design decision, not a patch, and it needs the owner") | land the `address` stamp as its own flagged step before the deletion, so the deletion is not blocked on a decision and the decision is reversible |

---

## Summary

### Fatal

1. **The plugin writer's whole-output diff and the `delta` scoping contradict each other (D1).** `run`
   must return the WHOLE desired output and the writer applies `DELETE current - desired`, while
   `delta` exists "so a scan can be scoped" and `P1` and `P3` scope theirs. A scoped run therefore
   retracts every edge outside its scope on every incremental pass. Repairing it costs a principle in
   either direction: full recompute per 50 ms flush breaks the cost budget of `11.1` (`P3` is today's
   entire fuzzy pass, `02 section 2.10`, run up to four times per flush instead of once per 100 ms
   debounce), and a scoped diff removes "idempotent by construction", which `5.3`, `9.3` and `11.3 I1`
   all rest on.

### Serious

1. The `6.1` page query does not compile: `uri` is unbound inside the `EXISTS` subquery, and `00`
   records that a binder error THROWS (A1).
2. The anomaly trace query is unconditionally true: `size()` over a JSON column that `00` says reads
   back as a string, so `'[]'` passes (A6).
3. The generic plugin writer cannot write `MediaProfile.startDay` or `EpisodeProfile.day`: `00` says a
   STRING param into a DATE column is a binder error (A7).
4. `P2`'s span scan is a cartesian product with no join predicate between the run side and the folding
   side, expanded over episodes, run every pass, while `11.1` claims every plugin is delta scoped (A9).
5. `P0` scans the append-only `Answer` log on a non-key column while the `ABOUT` edge declared for that
   join is never used (A10).
6. The read budget is met in query count and missed in bytes: `6.1` returns every member's `raw`, which
   `00` says comes back as a string, and the card's fields (covers, descriptions, genres, tags,
   popularity, relations) are projected nowhere else (A11).
7. Ingest, up to four plugin iterations, every subscription read and seven blocking extractors all
   share `00`'s single-threaded engine lane, and no cost is ever summed over one 50 ms window (A12).
8. The "250 rows is well under 50 ms" argument reads `00`'s 2,000-in-107 ms number backwards, because
   the loader batches 250 TOP-LEVEL medias and `recursivelyUnwrapMediaHandles` flatMaps each first
   (`src/worker/extractor.ts:107`) (B2).
9. The ingest cannot compute the `changed` set it emits: `MERGE` reports no newness and step 2 reads
   only `Media`, so today's boolean guard (`graph.ts:293-296`) is replaced by a full six-plugin pass
   per flush (B3, B4).
10. The container badge the design is built around cannot be drawn: a PART_OF-only placeholder is in no
    `Cluster`, so `ATTACHED_TO` never reaches it, and `8.1` assumes the opposite (C1).
11. `I14` is claimed to hold and does not: a CONTAINER cluster has slots, so `mergeByEpisodeNumber`
    runs over `paramount`, `trakt` and `tvdb` rows that flatten every season, which is the precondition
    `db.ts:465-468` forbids (C2).
12. `exportStore` over raw `CLAIMS` re-admits every cross-scope SAME_AS claim as sameness, which is the
    `01 case 1` weld, and the offline seed is built from that output (C3).
13. `view:changed` no longer wakes a source blocked on a field that arrives inside an existing row, and
    `04 section 3` explicitly requires that check for `waitForMedia`'s seven callers (C4).
14. `Media._id` is no longer monotone (a `plugin:aggregate` version bump retracts every cluster id, and
    splits are now possible) and `Slot.id` embeds it, so `Episode._id` inherits both (C5, D6).
15. The aggregated uri can now shrink, and it is a bookmark, a route and a party-synced path (C6).
16. The core's runtime proof that a plugin left source rows alone is `max(seq), count(*)`, which cannot
    see a `SET` on an existing row, and the verb checker is a parser problem solved with a word list
    against an engine that accepts multi-statement queries (D2).
17. Nothing refuses a `PART_OF` proposal over a pair an active `SAME_AS` already joins, so the owner's
    containment rule can be broken by which plugin ran first (D3).
18. A plugin that throws mid-run is unspecified, and a pass that aborts before `view:changed` leaves a
    page empty forever (D4).
19. `store/consensus.test.ts` is counted as KEEP verbatim while `9.1` deletes three of its four
    subjects, so the 55/18/1 split is overstated and the two episode defects it pins lose their tests
    (E1).
20. Step 1 is not a day, and what it says it ships (the trace story) needs tables that do not exist
    until step 2 (F1, F2).
21. Step 3 leaves the listing on the fuzzy-merged old store and the detail page on the direct-only new
    one, so a card and the page it opens disagree about membership and `_id` (F4).

### Minor

1. Map projection `m {.prop}` is used in four load-bearing queries and is absent from `00`'s verified
   list (A3).
2. Filtered variable length paths are written without the bound rel variable `00` verified (A4).
3. The `7.5` path trace stacks four unverified features (named path over a filtered recursion,
   `rels(p)`, a struct-building comprehension, a rebound `r`) (A5).
4. `MERGE`'s all-or-nothing rule pins `CLAIMS.node` to the claimer's first, poorest description (A8).
5. `Answer.selection` cannot be derived from the result object the hook sees (B7).
6. Removing the singleton branch of `aggregateMedia` changes every single-source card's uri, origin and
   url, and the migration does not name it (C10).
7. A third `MediaHandleRelation` value is a client-visible schema change (C11).
8. The four-iteration cap means a read can render a non-fixed-point graph, which qualifies `G3` (D5).
9. `P3`'s year bucketing collects years only from profiles that already have title keys, where
   `profileCluster` collects years from every member and skips the cluster only when it has no titles
   at all (`fuzzy-merge.ts:597-605`), so a priced pass changes its input silently.
10. `9.3`'s closure cross-check is capped at 8 hops against an unbounded union-find (E2).
11. `store/edge-idempotence.test.ts` as rewritten no longer covers the write path (E5).
12. Step 6 bundles the `address` decision, which `11.2` says the owner must make, with the deletion of
    the old store (F6).

### What clearly holds (do not re-derive)

- `useOnResolve` keyed on the named type, its three consequences, the three module-level DataLoaders
  and their 50 ms schedule, and the fan-out that discards payloads are all kept with their reasons
  named (`4.1`, matching `04 section 1.2` and `1.5`).
- `pendingClaims` is genuinely replaced by a query plus recomputation, which answers `02 section 2.4`'s
  missing expiry and `01 case 48`'s arrival-order dependence at once (B6).
- The field merge keeps `lastWriteLongestArray`'s rule verbatim and stops destroying the loser, which
  is what `04 section 5.1` demands of whatever replaces `store/graph.test.ts` (B5).
- The whole-store fallback, `applyMediaFilters` after the hide, the filter's strictness, search
  relevance and the sorts are kept verbatim (C8), and the store-wide hide is the more consistent of the
  two rules rather than a regression (C9).
- One guard chokepoint for every `SAME_AS`, with refusals stored as queryable edges, is what `01 case
  2`, `01 case 7` and `01 case 6` ask for and what the current three pair linkers cannot give (D7).
- Episode identity as one proven pair per episode, never an offset, refutes `01 R1` by construction and
  gives `01 case 15` its piecewise mapping (D8).
- The `Answer` log plus `fieldSeq` plus `supports` answer `02 section 5.1` and `5.2` in full, and
  replay-of-the-log-as-a-test is the only mechanism proposed anywhere that turns a production bug into
  a fixture (E4).
- Refusal assertions carry a control that must pass, which is the house check-the-checker rule applied
  correctly (E3).
- The scope ratchet as a view over the answer log removes `02 section 3` item 2's unrecoverable
  overwrite: the source's own last word survives on the row, and CONTAINER-once is derived (P0).
