# Refutation: proposal-evidence-first, through the engine and contracts lens

Scope: (a) every Cypher statement against `00`, (b) the ingest against `04` section 1, (c) the five
read contracts of `04` section 2 and `02` section 4, (d) the plugin contract, (e) the test plan against
`04` section 5, (f) the migration. Code citations are paths under `/home/banou/dev/stub`.

## Findings

| # | item | verdict | evidence | what would fix it |
| --- | --- | --- | --- | --- |
| 1 | **`Media.scope` is null for most rows, and null joins nothing.** Section 4.2 writes `scope: row.scope ?? null`, and the batch statement 1 of 4.3 sets `m.scope = r.scope` with no default. The RUN default lives in exactly one place today, `normalizeToStoreMedia` (`src/worker/store/normalize.ts:39`, `scope: ... ?? 'RUN'`, quoted at `02` section 2.12), and 9.2 deletes that file. With `scope` null the `claims` derivation table of 5.2 has no matching row (its six rows are all RUN/CONTAINER), so the claim is silently dropped, and `membership`'s scan requires `a.scope = b.scope`, which is UNKNOWN for two nulls, so the pair never joins. Most sources say nothing about scope (`03` section 1), so this is most of the graph | FIXABLE (one line, but every written query is built on the wrong value) | `02` section 2.2 quotes the ratchet as `scopeOf(uri) === 'CONTAINER' ? 'CONTAINER' : media.scope ?? 'RUN'`; the proposal keeps the first half and drops the `?? 'RUN'` | `ON CREATE SET m.scope = coalesce(r.scope, 'RUN')` and the same coalesce in the CASE; keep `Answer.scope` nullable so the per answer stamp still survives, which is the thing section 2.2 correctly wanted |
| 2 | **The `fold` veto query is a binder error, so `conn.query` throws.** `WITH a, b, ca, coalesce(l.value, max(cd.value)) AS ours` mixes a non grouped variable (`l.value`) with an aggregate inside one expression. `00` records that `conn.query` THROWS on a binder error rather than returning a failure flag, so this takes the whole plugin round down, not one rule | FIXABLE | 5.3 `fold`; `00` "conn.query THROWS on a binder error" | two statements: aggregate `max(cd.value) AS declared` grouped by `a, b, ca, l.value`, then `coalesce` in the next `WITH` |
| 3 | **Relationship `MERGE` is used in the one statement the proposal elsewhere refuses to use it in.** 4.3 statement 2 ends `MERGE (a)-[:ABOUT]->(m)`, while the note under statement 4 says relationship MERGE was not exercised on 0.20.4 and uses `NOT EXISTS` plus `CREATE` for `CLAIMS` for exactly that reason. Risk 2 names the assumption and then the DDL depends on it anyway | FIXABLE | `00` lists node `MERGE ... ON CREATE SET / ON MATCH SET` as exercised and says nothing about relationship MERGE; proposal 4.3 note and risk 2 | use the `NOT EXISTS` plus `CREATE` form for `ABOUT` too, or verify relationship MERGE first and then use it in both places |
| 4 | **`timestamp()` and `date()` on absent values are unexercised, and every `DateClaim` has at least one of the two columns empty.** `00` exercised `date($x)` only. A YEAR precision claim has no `day` and no `instant`; a DAY claim has no `instant`. `date(null)` / `timestamp(null)` inside `ON CREATE SET` is an unverified cast, and a throw rejects the whole ingest batch | FIXABLE | `00` "a STRING param into a DATE column is a binder error; write `date($x)` in the query"; proposal 2.1 `DateClaim(... day DATE, instant TIMESTAMP)` and 4.3 statement `x.day = date(d.day), x.instant = timestamp(d.instant)` | keep `dayNumber INT64` (which 2.3 already argues for) and drop the `DATE`/`TIMESTAMP` columns entirely, or split the UNWIND by precision so a null is never cast |
| 5 | **`from` and `to` as RETURN aliases, ordered on.** About a dozen scans do `RETURN a.uri AS from, b.uri AS to ... ORDER BY from, to`. `FROM` and `TO` are DDL keywords in this dialect (`CREATE REL TABLE R(FROM A TO B, ...)`, `00`), and `00` already records one alias trap (`ORDER BY` on an alias shadowing a node variable fails with `Order by NODE is not supported`), so alias parsing here is not a free assumption | FIXABLE | 5.2, 5.3 (five queries), 5.4, 5.5 | rename to `fromUri` / `toUri` everywhere, which costs nothing and removes a whole class of parse failure |
| 6 | **The `view` plugin is per row execution, which is the thing `00` prices at 18x.** 5.12 runs four queries per dirty cluster plus a fifth per ranged `INCLUDES`. On a cold load every cluster is dirty: `00`'s session graph is 1,000 clusters, so 4,000 to 5,000 round trips at the measured 1 to 2 ms floor, single threaded in the nested worker, before a single card renders. Risk 1 estimates "about forty statements per round", which is wrong by the cluster count | SERIOUS | `00` "2,000 nodes in 107 ms, against 935 ms one execute per row (18x)" and "a 60 cluster page with containers and episode counts in ONE query: 49 ms"; proposal 5.12 and risk 1 | `UNWIND $clusters AS cid` at the head of all four queries and build the JSON in JS from the returned rows; the 60 cluster join `00` measured is the shape to copy |
| 7 | **The listing read serializes the whole store on every event.** 6.1's fallback is `MATCH (v:View) WHERE NOT v.hidden RETURN v.clusterId, v.media`, and filters, search and sort all stay in JS, so every aggregated media (titles, descriptions, covers, banners, trailers, relations, franchise) crosses the worker boundary and is parsed on every 100 ms wake. Today `applyMediaFilters` receives in process object references and returns the same references, which `tests/unit/worker/store/filter.test.ts:20` pins | SERIOUS | `02` section 4.7, `04` section 2.1 (the whole store fallback is load bearing), `00` read costs | store two JSON columns on `View`: a narrow `card` (the fields `applyMediaFilters`, `searchRelevance` and the sorts read, plus `_id`, `uri`, titles, covers, popularity) for the listing, and the full `media` for the detail read |
| 8 | **Nothing renders until a full round has run, and there is no policy for a plugin that throws.** `view` is plugin 13 of 15 and `membership` mints even singleton clusters (5.4 step 4), so a page with no `View` row yields nothing (`04` section 6 contract 5). The applier commits one transaction per plugin (5.1), so a throw in `counts` leaves `membership`'s writes committed and no views at all, and the app is blank until the next ingest. Today a broken merge pass still renders unmerged clusters, because the write path and the aggregation are separate | SERIOUS | proposal 5.1, 5.1.2, 6.2; `04` section 6 contract 5 | state the policy: a plugin that throws is skipped with its output discarded and an `Anomaly` written, the round continues, and `view` runs over the last good derived state; plus a "no cluster yet" fallback view minted by ingest so a described media is renderable before any plugin ran |
| 9 | **The 100 ms trailing debounce in front of every round starves two consumers that have no debounce today.** `Subscription.media` listens with no debounce and `ctx.listenForMediaChanges` likewise; the seven `waitForMedia` callers block on it with 15 s and 30 s deadlines. `04` section 3 records that the debounce RESTARTS on each event, so "a continuously writing store starves the listing until writes pause for 100ms". Under a cold fan out of 24 sources the round may not run for seconds, and the sources that block are themselves part of the write stream | SERIOUS | `04` section 3 (listener table and the debounce mechanics), `04` section 1.8, `src/sources/utils.ts:488-508`; proposal 4.6 and 5.1 "100 ms trailing debounce" | run the round on a LEADING edge with a trailing catch up, or keep `graph:changed` (per commit, already specified) as the wake for `Subscription.media` and `listenForMediaChanges` and use `views:changed` only for the listing |
| 10 | **The digest cannot detect the mutation it is supposed to prove did not happen.** 2.6 is `count(a)`, `max(a.seq)`, `sum(size(a.hash))` and three `count()`s. `size(hash)` is the string LENGTH, which is constant for any fixed length digest, so a plugin that rewrote every `raw` and recomputed every `hash` passes unchanged; an in place property rewrite changes no count. It also runs only "in dev builds" and in tests, so it proves nothing about production. The facade is a string check over a dialect where `00` records multi statement `query("...; ...")` works, so a read shaped prefix is not a read | FATAL as a proof (the claim, not the architecture) | proposal 2.6 and 5.1 "How the core proves a plugin never touched a source row"; `00` "Multi statement `query(\"...; ...\")`" | drop the word proof for the digest and make it content sensitive and always on (`md5` over an ordered `collect(a.key + a.hash)`), reject any statement the engine's own prepare does not report as read only, forbid `;` in a facade statement, and lean on the real argument: `PluginOutput` is data and the applier is the only writer |
| 11 | **`count-sum` placing container episodes as SAME_AS episode handles is R1 with a different guard.** 5.9 says `count-sum` emits no `SAME_EPISODE` edges "so nothing downstream can mistake that for a per episode identity", while 5.12 step 4 makes every container episode placed through a ranged `INCLUDES` a SAME_AS episode handle, and 8.4 says "the view places Netflix handles on all 64 rows by position". `04` section 2.3 says the watch page and `episodeOriginIds` take SAME_AS handles only, so those become playable urls. R1's refutation is not about vote margins, it is "a wrong alignment is worse than a missing one, and nothing downstream can tell them apart" with 13 of 25 rows carrying a wrong playable url; an exact count sum does not exclude the Blue Exorcist mechanism (a special inside one season plus a short season elsewhere still totals) | FATAL for the rule as specified, and it contradicts the document's own principle 6 | `01` R1 (Blue Exorcist netflixid 70304252, position 14 a special); proposal 5.9 against 5.12 step 4 against 8.4; `04` section 2.3 | make 5.12 step 4 read `SAME_EPISODE`, not the range: a container episode may contribute a displayed number through a range, and may only become a SAME_AS episode handle when a per pair edge exists. Then `count-sum` is safe to turn on and 8.4 renders 64 numbered rows with a season level Netflix badge |
| 12 | **The `split` veto, on by default with no cost arm, vetoes on any short count from any answer.** The query is `MATCH (a)-[:COUNTED]->(ca:CountClaim) ... WHERE ca.value < l.value` with no `countKind` and no answer selection. `02` section 2.9 records that "twelve sources set `episodeCount = episodes.length`, so a catalogue that could only reach part of a run publishes a SHORT count as confidently as one that knows the whole run", and `01` I9 has kitsu publishing 22 and anilist 13 against a true 12. A single short LIST count from a search row therefore blocks a correct SAME_AS permanently. The same unselected `COUNTED` pattern is in the `fold` veto, where any inflated count vetoes | SERIOUS | proposal 5.3 `split` and `fold`; `02` section 2.9; `01` I9 | restrict both to `countKind:'DECLARED'` from the candidate's own page answer, and run the arm before shipping a default on veto, which is what I2 and every threshold in `02` section 2.10 required of their own rules |
| 13 | **The scope ratchet can break a join that already happened, and nothing retracts it.** `membership` joins on `a.scope = b.scope`, then the ratchet can flip a member to CONTAINER later; retraction triggers are only "`SAME_AS` retracted" or "a `VETO` appeared" (5.4), neither of which a scope change is. So the invariant `MATCH (a)-[:JOINS]-(b) WHERE a.scope <> b.scope RETURN count(*)` does not return 0 "after every round", and it fails on exactly the measured case: a RUN row for `cr:G24H1N3MP` minted by justwatch milliseconds before crunchyroll said CONTAINER | SERIOUS | `02` section 2.4 (the justwatch / crunchyroll race, verbatim), proposal 3.4 and 5.4 | make a scope change dirty the member and its cluster, and have `membership` retract any `JOINS` whose ends now disagree and recompute the fragments; that path already exists for vetoes |
| 14 | **Retraction breaks `_id` stability and the only grows `uri`, and the proposal never prices it.** 5.4 says the largest fragment keeps the cluster id and the others get new ids. `_id` is the urql graphcache key for `Media` (`src/urql-keys.ts:17`), so a split replaces a card's DOM node mid session; and the aggregated `uri` SHRINKS, which `shouldGrowAddress` (`src/utils/uri.ts:143-149`, verified: returns false unless `inHand.length > named.length`) refuses to follow, so the modal keeps an address naming members its cluster no longer holds. Today a union has no inverse, which is why neither can happen | SERIOUS | `04` section 6 contracts 1 and 2, `src/urql-keys.ts:17`, `src/utils/uri.ts:143-149`; proposal 5.4 and 9.5 I1 | keep a `canonical` tombstone in BOTH directions so a retired id resolves to whichever fragment holds the uri that was asked for, and state what the UI does when an address shrinks (probably: keep the address, let the view answer with the smaller cluster) |
| 15 | **The hash newness test is strictly more sensitive than today's, so a volatile field wakes a round on every re-fetch.** `upsertMedia` sets `changed` only on a NEW row, a new union or a new PART_OF edge (verified at `src/worker/store/db.ts:141-151,190-196`), so a re-fetch with different field values emits nothing. `Answer.hash` over `raw` changes when any byte changes: a countdown in `nextAiringEpisode`, a signed url, a search rank. With the DataLoader flushing every 50 ms and `mediaPage` re-asking every origin, that is a permanent round loop, which is the failure `tests/unit/worker/store/edge-idempotence.test.ts` exists to stop | SERIOUS | `04` section 3 and section 6 contract 7; `02` section 2.7; proposal 4.3 and 4.6 | hash the projected evidence plus the claim set, not `raw`; keep `raw` replaced silently and emit only when an evidence row, a claim or a `described` flag actually moved |
| 16 | **`exportStore` over raw `CLAIMS` is a wider walk than the contract.** `export.ts` BFSes `ASSERTED_LABELS.RUN`, which `db.ts:189-195` writes only for a claim that was DERIVED as same scope sameness; a cross scope claim writes `ASSERTED_LABELS.PART_OF` instead and a RUN x RUN PART_OF claim likewise. 9.2 says the export walks "`CLAIMS` of kinds other than `answer`", which is every claim as sent, including ones the derivation turned into containment and ones a veto refused. The seed corpus would then carry welds the store never made, and `offline/seed-gate.ts` cannot see that because it checks shape, not policy | SERIOUS | `02` section 4.8, `src/worker/store/db.ts:187-197`, `04` section 6 contract 10; proposal 9.2 | walk `SAME_AS {strength:'ID'}` (the `claims` plugin's verdict) rather than `CLAIMS`, keep the cluster drop rule and the total ordering so two calls stay byte identical, and keep `offline/seed-build` as REWRITE rather than KEEP until the content is diffed |
| 17 | **`Answer.key = by|kind|about` loses the distinction it was built to keep.** `useOnResolve` keys on `getNamedType(info.returnType).name` (verified at `src/worker/extractor.ts:473-490`), so the resolved FIELD names of named type `Media` are `media`, `nodes` (inside `MediaPage`), `node` (inside `MediaHandle`) and `similarMedia`, not the documents 2.2 lists. Two documents that share a field name and differ in selection set collide on the key and the later truncates the earlier, which is exactly I5 point 3, the class 4.4 claims to close. The nested `handle` kind is worse: every nested node of every document collapses into one key per (source, uri) | SERIOUS | `04` section 1.2 and 1.3; `src/worker/extractor.ts:473-490`; proposal 2.2, 4.4 | key on `by | operationName | fieldPath | about`, or `by | hash(selection set) | about`; `info.operation` is available in the same hook |
| 18 | **Two rows with the same key inside one batch resolve by position in the UNWIND.** `mediaInserter` flatMaps `recursivelyUnwrapMediaHandles` over up to 250 medias (`src/worker/extractor.ts:106-134`), so one batch routinely carries the same handle node twice. `MERGE ... ON MATCH SET` applied twice in one `UNWIND` leaves the last row's `raw`, which is arrival order inside the batch, against principle 7 | FIXABLE | `04` section 1.3; proposal 4.3 statement 2 | dedupe in JS before the UNWIND with a stated rule (most fields wins, tie to the deeper document), the same way `handlePairs` is already deduped today |
| 19 | **One malformed row now rejects the whole shared batch.** The ingest is a multi statement transaction built from the projection, `conn.query` throws on a binder error (`00`), and the batch spans every extractor that flushed in the same 50 ms window. `04` section 4.3 records this exact failure in production on 2026-09-05: a bare row in `handles` made "THE SHARED INSERT BATCH REJECTED FOR EVERY EXTRACTOR IN IT, first party sources included". The design increases the blast radius and never restates a quarantine | SERIOUS | `04` section 4.3; `00`; proposal 4.3 | project in JS with total functions, validate each row before it enters the UNWIND, drop and record a row that fails, and split the transaction per source so one source's shape cannot take the others down |
| 20 | **Six veto scans, the `fetch` scan and every anomaly query have no dirty filter, and the ones that do use `list_contains` over a list that is thousands long on a cold load.** `00` prices a bounded candidate scan at 35 ms; the `format` veto collects a cross product of a's answers by b's answers for every `SAME_AS` pair in the graph, undirected so each pair twice, every round. The schema already carries `seq` on every row and 2.1 says that is what lets a plugin scan only what moved, and then no query uses it | SERIOUS | `00` read costs; proposal 2.1, 5.3, 5.13, 5.14 | filter on `s.seq > $since` / `m.seq > $since` instead of `list_contains($dirty, ...)`, and give the `format` veto an evidence row (a `FORMAT` claim table) instead of `json_extract` over every answer |
| 21 | **`Facet` keyed on `Title.norm` loses the per raw title ordinal that the season veto depends on.** `02` section 2.10 records that ordinals are parsed from RAW titles "because normalizeTitle folds `Season 4` and `Season 40` closer together and drops the delimiters `第 4 期` is allowed to carry". 5.5 keeps one `Facet` per norm and drops `part` (and by the same rule the ordinal) unless every raw text under that norm agrees, so two spellings that normalize together silently disable gate 2 for both, globally, across unrelated media that share the norm | SERIOUS | `02` section 2.10 `profileCluster.seasons`; proposal 2.1, 5.5 | put `ordinal`, `part`, `onlyLabel`, `generic` and `companion` on the `HAS_TITLE` edge, which already carries `text`, so a facet stays per answer and per raw spelling |
| 22 | **`MEMBER_OF` uniqueness is assumed by every read and enforced by nothing.** 6.1, 6.2 and `Media.episodes` all assume one cluster per media; the engine has primary keys on nodes only and no uniqueness on a rel table (`00`), and `membership`'s rewrite path is a delete plus insert that can leave a stale row. It is not one of the three invariant queries of 3.4 | SERIOUS | `00` DDL surface; proposal 3.4, 5.4, 6.1, 6.2 | add `MATCH (m:Media)-[:MEMBER_OF]->(c) WITH m, count(c) AS n WHERE n > 1 RETURN count(*)` to the invariant set and run it in the same place as the other three |
| 23 | **Plugin disagreement is resolved by round order, which is arrival order wearing a hat.** `membership` refuses a join when a `PART_OF` or `INCLUDES` exists between the two clusters (5.4 step 2), but nothing retracts a join when that edge arrives LATER, since retraction fires only on a retracted `SAME_AS` or a new `VETO`. So `title-match` proposing `SAME_AS` in round N and `similar` proposing `PART_OF` in round N+1 leaves the join standing, while the reverse order leaves it refused | SERIOUS | proposal 5.1 principles, 5.4, 7; principle 7 "No decision reads arrival order" | treat a new `PART_OF` / `INCLUDES` touching two joined members as a retraction trigger, listed next to the veto trigger |
| 24 | **The round DAG has back edges and an unproven fixpoint.** `similar` (5) consumes `RunLength` produced at 8; `containment` (10) consumes `Ask` produced at 15; step 9's one iteration bound is argued within a round only. Across rounds the cycle `membership -> counts -> vetoes -> membership` can oscillate: a fold veto retracts a join, `RunLength` changes, the veto's premise disappears, the next round re-joins, and each flip emits `views:changed` and re-renders | SERIOUS | proposal 5.1.2 step 9, 5.3, 5.10 | compute vetoes against a length that does not depend on the join under test (the pair's own declared counts, which the `coalesce` fallback already does), or freeze `RunLength` inputs per round and prove the monotone direction |
| 25 | **9.2 and 9.3 contradict each other on the 31 consensus tests.** 9.3 carries `store/consensus` verbatim; 9.2 turns `alignRunEpisodes` from "a Map of renumbered copies" into edges and deletes the `Media` store row type that `runEpisodes(cluster: Media[])` and `runLength(cluster)` take as their first argument. `04` marks the file KEEP because the functions are pure, which stops being true when the return type changes | FIXABLE | `02` section 2.9, `04` section 5.1; proposal 9.2 against 9.3 | keep `alignmentOffset` and `runEpisodes` as pure functions over row shaped inputs and pin them unchanged, and write the edge minting as a thin caller in `range`; then the KEEP is honest |
| 26 | **The test plan asserts an invariant its check cannot express.** 9.4 has every plugin test assert the digest of 2.6 after every run. Given finding 10, that assertion passes unconditionally, which is the most expensive shape of check in this codebase's record | SERIOUS | proposal 9.4; `02` section 5 (the whole provenance argument) | pair the digest assertion with a control that must fail: a deliberately misbehaving plugin in the harness that rewrites one `Answer.raw` and must be caught |
| 27 | **Step 1 is not a day.** It is `engine.ts` plus the 22 MB worker path wiring, the full DDL, `splitAnswers`, the whole EVIDENCE projection (date precision by shape, script detection, the language map, a `countKind` table for about twenty sources and a `numberSpace` table for every origin, none of which is declared anywhere today), the hash read back, the digest, `?export=graph`, the core suite and a rig smoke test | FIXABLE | proposal 2.3, 10 step 1; `03` sections 1 to 3 | split it: engine plus DDL plus a smoke test first, then `splitAnswers` and RAW, then the projection tables one source family at a time, each with its own test |
| 28 | **Step 6 flips the flag and deletes the old store in the same step.** That is the one step with no rollback, and it lands after five steps whose whole argument was that the app keeps running | FIXABLE | proposal 10 step 6 | flip, bake, then delete in a separate commit |
| 29 | The `SHOW_LEVEL_ORIGINS` backstop (`imdb` reads as CONTAINER whatever the row said) is named in prose at 2.2 but is absent from the only written statement, 4.3 statement 1, which consults `r.scope` and `m.scope` only and never `Origin.models` | MINOR | `02` section 2.1 (`db.ts:42,91-92`); proposal 2.2, 4.3 | compute `r.scope` in the inserter from the `models` table, and pin it where `store/db.test.ts` pinned it |
| 30 | View query 4 returns `a.raw` (a JSON blob) alongside `collect(DISTINCT e2.uri)`, making the blob an implicit grouping key | MINOR | `00` (JSON is native and reads back as a string); proposal 5.12 | return `e.uri` and the `same` list in one query and the raws in another, joined in JS |
| 31 | `ORDER BY json_extract(v.media, 'startDate')` is not `preferAttachedRun`, which orders by earliest PARSEABLE start date and then by `keyOf`, so an absent or unparseable date must not sort first | MINOR | `02` `db.ts:319-323,338`; proposal 6.2 | materialize `View.startDay INT64` (null last) and order on it, then `rc.id` |
| 32 | Risk 6 mis-states today's behaviour: `hideAttachedContainers` already runs on the whole unfiltered list before `applyMediaFilters` (`src/worker/resolvers/media/index.ts:126-137`), so a container whose run is filtered off is already hidden today on the whole store path. The change only affects the `insertedUris` path | MINOR | `02` section 4.5, `04` section 2.1; proposal risk 6 | restate the risk narrowly |
| 33 | `Media` gains `fields` (an object typed field) and `kind`, and 9.1 says `urql-keys` is untouched. Every other embedded object type in the app is keyed `null` in `src/urql-keys.ts:22-30` so graphcache embeds it; a new object type without an entry is treated as a keyable entity | MINOR | `04` section 2.3 and section 5.5; proposal 6.4, 9.1 | add `MediaFields` to the keys config and to `tests/unit/urql-keys.test.ts` |
| 34 | `fetch` (5.13) mints one `Ask` node per uri per session for every uri any source ever named in a handle, with the interest rule applied later by the actuator, and its `NOT EXISTS { MATCH (:Ask {kind:'MEDIA', uri: m.uri}) }` is a scan of `Ask` per candidate because `Ask`'s primary key is `key` and the engine has no secondary index | MINOR | `00` (primary key only); proposal 5.13, 7 | give `Ask` the primary key `MEDIA|<uri>` so the existence test is a key lookup, and apply the interest filter in the scan rather than in the actuator |

## Summary

### Fatal

1. **The stated proof that a plugin never touched a source row is not a proof** (finding 10). `sum(size(a.hash))` is invariant under any content change at fixed hash length, the counts are invariant under an in place rewrite, and the check runs only in dev builds and tests. The facade is a string check over a dialect where `00` records that multi statement `query("...; ...")` works. Principle 2 says "the core proves it with a digest that must not move"; the digest as written cannot move for the mutation it is aimed at. The architecture is fine; the claim has to be replaced by a content sensitive, always on digest plus a parse level facade, and by resting on the applier being the only writer.
2. **`count-sum` plus 5.12 step 4 ships R1's outcome** (finding 11). The document says positional placement cannot be mistaken for per episode identity and then makes every positionally placed container episode a SAME_AS episode handle, which `04` section 2.3 turns into a playable url on the watch page. `01` R1 refused exactly this on live data, and the refutation it names (a one vote margin) is not the mechanism it is guarded against; an exact count sum does not exclude an intra season insert paired with a short season. This also breaks the document's own principle 6. One table row fixes it: a container episode may contribute a number through a range and may only become a SAME_AS handle where a `SAME_EPISODE` edge exists.

### Serious (wrong or missing, fixable inside the design)

- Scope defaults to null instead of RUN, so the derivation table has no matching row and `membership`'s `a.scope = b.scope` never matches for two silent rows (1).
- The `view` plugin is per cluster execution, 4,000 plus round trips on a cold load, against `00`'s 18x per row penalty and the document's own "forty statements per round" (6).
- The listing serializes every cluster's full aggregate on every event because filters stay in JS (7).
- No error policy for a plugin that throws, and the read path sits downstream of all fifteen (8).
- The 100 ms trailing debounce in front of the round starves `Subscription.media` and the seven `waitForMedia` callers, which have no debounce today (9).
- The `split` veto vetoes on any short count from any answer, against `02` section 2.9's twelve sources that publish `episodes.length` (12); the `fold` veto has the same unselected `COUNTED` pattern.
- A scope ratchet after a join leaves a cross scope `JOINS` with no retraction path, so the invariant that must "return 0 after every round" fails on the measured justwatch / crunchyroll race (13).
- Retraction changes `_id` and shrinks the aggregated `uri`, against `04` contracts 1 and 2 and `shouldGrowAddress` (14).
- The hash newness test fires on any byte change where today only a new row, union or edge fires (15).
- The export walks raw `CLAIMS` rather than the applied same scope pairs `ASSERTED_LABELS.RUN` records (16).
- `Answer.key`'s `kind` is the resolved field name, so documents sharing a field name collide, which is the truncation class 4.4 claims to close (17).
- One malformed row rejects the shared 250 row batch, the failure `04` section 4.3 already measured in production (19).
- Most plugin scans have no incremental filter, and `seq` exists for exactly that and is never used (20).
- `Facet` keyed on the shared normalized title loses the per raw ordinal `02` section 2.10 requires (21).
- `MEMBER_OF` uniqueness is assumed everywhere and enforced and checked nowhere (22).
- Plugin disagreement resolves by round order (23), and the veto / counts / membership cycle has no proven fixpoint across rounds (24).
- The plugin tests assert a digest that cannot fail (26).

### Minor

Relationship `MERGE` used where the document itself refuses it (3); `date()` / `timestamp()` on null (4); `from` / `to` as aliases (5); intra batch key collisions resolved by position (18); 9.2 against 9.3 on the consensus tests (25); step 1 is not a day and step 6 has no rollback (27, 28); the imdb backstop missing from the written statement (29); a JSON blob as a grouping key (30); `preferAttachedRun`'s ordering (31); risk 6 mis-states today (32); `MediaFields` and urql keys (33); `Ask` primary key and the interest filter (34).

### Holds (the hardest cases it clearly handles)

- **Episodes only arrive through a resolved `episodes` field** (`04` section 1.2, 10 rows against 0): 4.5 uses that deliberately rather than fighting it, and keeps the `similarMedia` document selecting no episodes so a `containing` answer lands without an episode list.
- **`pendingClaims` becomes structural.** `described` on the node plus `a.described AND b.described` in the `claims` scan reproduces the defer and replay of `02` section 2.4 with no map, no expiry question and no arrival order, and a placeholder still records what was rebroadcast.
- **`lastWriteLongestArray`'s destructive merge is genuinely removed** by replace within key and keep across keys: a search row and a page row about one uri coexist, which is `02` section 2.5's losing side and I5 point 3.
- **The read path does not recurse.** Every read joins `MEMBER_OF` and a primary key (`00`: 1.7 ms against 6.8 ms, and 1,033 ms on a dense graph), the single recursive query is confined to the membership split, and `componentId` minting during a read (`02` section 2.6) is gone.
- **Ingest batches with `UNWIND`**, which is `00`'s 18x.
- **The scope derivation table of `02` section 2.1 is carried verbatim**, including the CONTAINER x RUN direction flip, and the three invariant queries of 3.4 make the owner's "never traverse containment as identity" rule structural rather than a convention, because `JOINS`, `PART_OF` and `INCLUDES` are different rel tables and a pattern cannot cross them.
- **Ranged `INCLUDES` plus one `SAME_EPISODE` per aligned pair** makes a mid season insert cost one episode its link and shift nothing, which is the property `01` case 15 needs and `alignmentOffset` already has.
- **Tiered consensus is carried verbatim with no sum** (`02` section 2.8, R3), and the two witness bar and the spared sets of `runEpisodes` survive into the view's windowing.
- **The read surface is unchanged**: four subscription fields plus `Media.episodes`, `Query` and `Mutation` empty, `applyMediaFilters` and its test kept verbatim, `SEARCH_RELEVANCE_THRESHOLD = 0.7`, the inverted sort names left alone, the whole store fallback kept as a fallback.
- **The event payload finally carries ids**, which `04` section 3 records as declared and never populated, so a page can re-read one cluster instead of the store.

### Score

5 / 10. The schema, the band split and the "nothing unions" decision are right, and the contract inventory is unusually faithful. But as written the ingest computes the wrong scope for most rows, at least four statements would throw on the measured engine, the round costs two orders of magnitude more than the document's own estimate on the path a user feels, the isolation proof cannot fail, and one default rule plus one view rule combine to re-ship a refuted alignment as playable urls.
