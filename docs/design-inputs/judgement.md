# Judgement: the three proposals for stub's new representation

Judged against the owner's brief and the five inputs (`00` engine facts, `01` cases, refutations and
invariants, `02` current store, `03` source capabilities, `04` consumers and tests), with the six
refutations in hand. Four code facts that several verdicts turn on were re-read in the tree and are
cited by `file:line` where they decide something.

The verdict in three lines: **product-first is the base.** Its skeleton (an append-only answer log with
an owner rule on the merged row, one writer with a guard list and a downgrade, six plugins that map onto
the brief's named defaults, a slot read that cannot be handed a container, and a trace panel) needs the
least surgery. It takes fourteen grafts, most from evidence-first (the `containing` answer, the split
veto, recorded silent gates, the invariant queries, the rewired exchange-rate rigs, typed evidence
declarations) and three from minimal-core (the scoped reconcile contract, content-hashed answers, claim
precedence classes). Six owner decisions remain, all of them changes to the schema or to the plugin
contract.

---

## 1. Scoring

Scores 0 to 5 per cell. Sections 2, 3, 5, 6 and 8 count double (they are the design). Maximum 80.

| section | minimal-core | evidence-first | product-first |
| --- | --- | --- | --- |
| 1. Principles (x1) | **4**: eight rules each tied to a case; P3 ("nothing in the core is irreversible") is stated wider than `04 section 6` contracts 1 and 2 allow | **4**: "absent, zero and unknown are three values" is the sharpest principle of the set and its schema delivers it (`01 case 23`); principle 2 promises a proof the digest cannot give | **5**: five read guarantees named from the user's own surfaces plus eight rules, the closest to the brief; principle 6 ("never on arithmetic") is the one its own P4 rule 3 breaks |
| 2. Graph schema (x2) | **3**: the leanest table set and the best claim provenance (`CLAIMS` FROM the payload that carried it); but `LINKS` is never declared over `Cluster` pairs (a binder error, `00`), `handles` and slot `members` are four-field JSON no fragment can read (`04 section 2.3`), and an anchor with no payload can never be a member (`01 case 48`) | **4**: typed evidence nodes make agreement a Cypher join and `Origin.models` gives imdb a row (`01 case 42`); the `by\|kind\|about` key collides across documents sharing a field name, `scope` lands null, and the EVIDENCE band carries vocabularies a plugin cannot retract | **4**: the owner rule on `raw` plus the `Answer` log with `fieldSeq` close `01 I5.3` outright and the profile is a retractable plugin; DATE columns the generic writer cannot fill (`00`), `titleKeys` loses the per-title score `01 case 30` measured, a PART_OF-only placeholder sits in no cluster |
| 3. Edge model (x2) | **4**: the range form as literal properties with witnesses per segment answers `01 case 15`; the showcased filtered path matches nothing identity writes, and `ALIGNS` duplicates what a per-episode pair does | **5**: rangeless `INCLUDES` is "containment allowed to be partial or absent" (`01 case 9`), direction by superset covers the split (`01 case 10`), `VETO` and `JOINS` are first-class, three invariant queries make the owner's rule structural | **5**: `INCLUDES` labels and bounds, `EPISODE_LINK` carrying the shared day places the button (`01 R1` refuted by construction), refused links stored with reasons, `supports` chains to the answer; guard 6 is one-directional |
| 4. Ingest (x1) | **3**: anchors on first naming retire `pendingClaims` cleanly (`02 section 2.4`); the content hash is unstable under array order, claims carry no ordinal, the ASK payload writes no `DESCRIBES`, the dual write can reject the shared batch (`04 section 4.3`) | **3**: replace-within-key ends the truncation class (`01 I5.3`); `scope` defaults to null so the derivation table matches nothing, one malformed row rejects the batch, the hash over `raw` wakes a round on any byte, relationship `MERGE` is used where the text refuses it | **4**: a serial queue, the `Answer` log, the owner rule, and `lastWriteLongestArray` kept verbatim with the loser archived (`04 section 5.1`); the `changed` set cannot be computed as written and "250 rows" counts top-level medias only |
| 5. Plugins (x2) | **3**: `reconcile` by natural key with a scope is the best write contract of the three; C4 is incoherent on the record's own counts, the check disables identity under normal load, no termination argument, the window drops the spared sets (`01 case 45`), I6 arbitrates by hash | **4**: vetoes as recorded objections with `skipped:absent` (`01 case 6`), the global id contradiction check (`01 case 11`), the split veto, anomalies at runtime; the digest cannot fail, `title-match` never mints the cross-scope `PART_OF` its walkthrough needs, rule B is dead for Crunchyroll, `episode-number` regresses `01 case 19`, `view` runs per cluster | **4**: one writer with eight guards and the downgrade states the RUN versus CONTAINER exchange once (`02 section 3` items 4 and 5 closed), run length with classes and the spared sets kept, slots through `FILLS`; whole-output diff contradicts delta scoping, rule 3 mints SAME_AS on a count, an undowngraded refusal creates a card |
| 6. Read path (x2) | **3**: reads are lookups of `Aggregate` and `EpisodeSlot` and the container page falls out for free; `handles` cannot satisfy `MediaFragment`, `attached` is NULL-unsafe and global, `waitForMedia` waits on a pass with no budget | **3**: `View` rows with kind `FOLD` and `canonical` resolution; the listing serializes every cluster on every event, nothing renders before plugin 13 of 15, the debounce starves the undebounced `media` subscription (`04 section 3`) | **4**: resolve, members, slots matches today's contracts including `preferAttachedRun`'s tiebreak, the slot read cannot be handed a container (`04 section 2.1`), the rendering table per page kind; the page query does not compile as written and returns `raw` per member |
| 7. Trace loop (x1) | **3**: the stop table is complete but the only bound is a cap of three passes | **4**: `Ask` as data with an outcome, the interest rule written down, a session bound stated; `01 R15` re-committed by one MEDIA ask per uri and the ask cap miscounts | **5**: the prohibition walked end to end for cour 2 against Netflix season 1, and the "why is this here" panel answers every loss `02 section 5` lists; re-keying `askUnasked` on cluster ids is a wording slip (`media/index.ts:58-65` keys on origins) |
| 8. Walkthroughs (x2) | **3**: 8.3, 8.4 and 8.5 are right; 8.1 asserts a C4 window that is not unique on `01 group B`'s own counts (11, 12, 12, 12, 14 give pair sums 23, 24, 24, 26), and 8.2 demotes a member the shipped code keeps | **3**: the richest row tables; three claims are false against the record (a DAY start date on a Crunchyroll season row, `crunchyroll/extractor.ts:189`; kitsu joining anizip under its own `episode-number` query; an FMA decomposition `01 case 10` does not carry) | **4**: pass by pass with what the user sees; Netflix minting nothing for Mushoku is traced correctly (`01 case 37`); "three declared witnesses" contradicts its own tier rule and `aligned` membership churns while a season airs |
| 9. Kept, goes, tests (x1) | **3**: a complete export table; `consensus.test.ts` cannot be KEEP once `alignmentOffset` is generalized, the corpus replay is merged rows not payloads, the `04 section 5.5` suites are missing | **4**: the exchange-rate scripts rewired to the harness and a corpus with expected `Refusal` rows; 9.2 and 9.3 contradict on the consensus tests and the export walks raw `CLAIMS` | **4**: the `Answer` log replay turns a production bug into a fixture, every `01` case a fixture and every R a negative fixture, refusals asserted with a control; `consensus.test.ts` counted KEEP while its subjects are deleted |
| 10. Migration (x1) | **3**: dual write, a flag, a differential replay; step 1 is two to three days and the dual write can reject the shared batch | **3**: shadow with a cluster diff overlay, source changes last; step 1 carries the whole projection and step 6 flips and deletes in one commit | **3**: tee, diff report, one read at a time; step 3 runs two merge policies at once so a card and its page disagree, step 6 bundles the address decision with the deletion |
| 11. Risks and decisions (x1) | **4**: risks marked measured or not; six decisions, including COUNT position lending named against `01 R1` | **4**: ten decisions including third-party merge plugins and whether ID claims face the soft gates; two risks are the design's own unmeasured rules | **4**: eleven risks with mitigations; seven decisions including where the 22 MB engine loads; the invariants table |
| **Total (of 80)** | **52** | **60** | **67** |

Weighted arithmetic: minimal-core 4 + 6 + 8 + 3 + 6 + 6 + 3 + 6 + 3 + 3 + 4; evidence-first
4 + 8 + 10 + 3 + 8 + 6 + 4 + 6 + 4 + 3 + 4; product-first 5 + 8 + 10 + 4 + 8 + 8 + 5 + 8 + 4 + 3 + 4.

---

## 2. The fatal findings, one by one

Sixteen findings were marked FATAL by a refuter: nine on minimal-core, three on evidence-first, four on
product-first. Each is ruled on below. "Shared by" names the other proposals that carry the same defect
under their own rules, which is what decides whether a finding is about a design or about the problem.

| # | finding (refuter) | ruling | shared by |
| --- | --- | --- | --- |
| MC-1 | C4, the count-exact fold, cannot fire as written and, under its only coherent reading, its own walkthrough is wrong: JustWatch's 24 matches two windows of `01 group B`'s cours (cases refuter, `01 case 9`, `01 I6`) | **Fatal for the rule. Upheld.** Count partition is `01 R20`'s offset composition with counts as the lookup table, and any relaxation that makes it useful mints a playable Netflix url on a wrong cour page (`01 I2`). The rule is deleted; a count alone never mints an `INCLUDES` range | evidence-first `count-sum` (off by default) plus its view step 4 (EF-3); product-first P4 rule 3 (the count-exact SAME_AS, PF-1). All three carry a count-only road to a play button; the final design closes it in one sentence: **a per-episode SAME_AS handle exists only where a per-episode pair with evidence exists** |
| MC-2 | An accepted SAME_AS to an anchor with no payload disappears: film-level provider links (six origins exist only to render them, `03`) and watchmode's handles vanish because `MEMBER_OF` needs a payload and `handles` is built from members (cases refuter, `01 cases 40, 42, 48`) | **Fatal as written, fixable.** The PART_OF half already renders from `CLAIMS.node`; the SAME_AS half must too | product-first partially: a placeholder SAME_AS member is admitted but 6.3 never says where its `url` comes from (the claimer's `node.url`, as for containers). Evidence-first is clear of it: a JustWatch offer node carries `url`, so `described` is true and it joins |
| MC-3 | The loan now requires dated episodes on the run side, which most runs lack; today `runEpisodes`' window lends `1..length` by position with no date (cases refuter, `01 case 12`) | **Overruled.** The positional loan is right only when the run is the container's FIRST cour. For cour 2 of a folded season the window keeps the container's episodes 1..12, which are cour 1's, on cour 2's page: a wrong row with a playable url, which `01 I2` forbids. The record's measured win (`01 case 12`, both part-twos 0 to 12 Crunchyroll sources) was produced with anizip dates present, so pairs reproduce it. Dropping the positional loan is the correct side of the exchange; the cost (no per-episode Crunchyroll on a run with no dated episodes of its own) is stated in the design | all three take this side; the refuter's `COUNT_PREFIX` edge is not adopted |
| MC-4 | The episode window drops the spared sets, so AniList's thirteenth episode is deleted where `consensus.ts:210-211` keeps it (cases refuter, `01 case 45`, `01 I9`) | **Fatal for the rule, fixable.** Product-first and evidence-first both keep `reference` and `equals` (verified at `consensus.ts:210-211`); the final design keeps them | nobody else |
| MC-5 | `01 case 23` (no count read as zero) is claimed and not delivered; the defect lives in seven source call sites reading `episodes.length` off an aggregate that always emits `[]` (cases refuter) | **Overruled as a design fatal.** It is a source-side fix (read `episodeCount`, never `episodes.length`) that every proposal must list in its migration; the store's contribution is `countKind: none` (product-first P0) and guard 8. Downgraded to a misclaim plus a migration item | all three, as a source-side item |
| MC-6 | The check disables `plugin:identity` on a cold load: a DataLoader flush lands during the pass (every call is an await, `00`), the source counts move, and 5.4 deletes the plugin's output and disables it (engine refuter D1) | **Fatal as written, one-line fix**: audit rows with `seq <= passStart` only, and audit by content hash rather than count (see EF-2) | product-first's audit (`max(seq), count` per table after each pass, 5.3) under a scheduler that admits emits mid-pass; evidence-first's digest "after every plugin transaction". All three; the final design states the seq bound |
| MC-7 | No termination argument: the monotone union-find was what made the read, merge, re-read loop terminate (`02 section 2.7`, `04 section 2.1`); a cap of three passes plus an idle re-trigger admits a permanent oscillation (engine refuter D3) | **Fatal as written. Upheld, and it is the one finding that costs every proposal a principle.** The fix is a rule, not a number: **a SAME_AS link once active is sticky; it is retracted only by a guard whose evidence changed (a disagreeing id, a containment edge, a scope flip, a date), never because the merged cluster's profile no longer matches.** Contradicting evidence is finite (each source row lands once), so links only accumulate within a session except on contradiction, which is the monotonicity argument restored. A per-iteration state hash detects a cycle and reports it as an anomaly | product-first (cap of four, plus `aligned` membership that flips weekly while a season airs); evidence-first (`membership`, `counts`, `vetoes` back edges, finding 24). All three |
| MC-8 | Retraction against `04` contract 2: `shouldGrowAddress` follows only a growing address (`src/utils/uri.ts:143-149`, verified: `inHand.length > named.length`), and urql keys on `_id`; a routine split strands the address and remounts cards (engine refuter C1) | **Real, shared, not fatal to the architecture once MC-7's sticky rule makes retraction rare.** It needs a published-identity rule: the aggregated uri is append-only per cluster id (a departed member stays in the address as an alias the resolve accepts), `Cluster.aliases` maps retired ids both ways, and a resolve by `_id` that lands on a cluster not holding the requested member falls through to the member uri | product-first C5 and C6; evidence-first finding 14. All three |
| MC-9 | `useOnResolve` keys on the named type and `MediaHandle.node` is `Media!`, so nested handle nodes reach `mediaInserter` as payloads under the answering server's origin, which is `01 I5.3` exactly (engine refuter B1) | **Confirmed in the tree** (`src/worker/extractor.ts:473-479`; `schema.gql:226-231`). **Not fatal to any architecture, but required in every ingest**: the hook must key on position as well as type (only a top-level result of the subscription root or `MediaPage.nodes` is a payload; nested nodes reach the graph through the parent's handle walk), with a test whose control is a nested node that must NOT produce an answer row. Product-first's owner rule already makes the double firing harmless for foreign-origin nodes; the hook change removes the log noise and the same-origin ambiguity | all three keep the hook "unchanged" (9.1 in each) |
| EF-1 | `01 case 39` cannot be expressed: `Media.uri` as the key collapses `tmdb:550` the film and the series into one node (cases refuter) | **Overruled.** All three key on the uri and the uri is also the route and the party-synced path (`04 section 6` contract 2), so an `(origin, kind, id)` key would change every address. Parity with today: the refusal stays on the claimant (simkl and watchmode refuse; `trakt/extractor.ts:79` is `01` open problem 17, a source fix). The final design adds the cheap half: a SAME_AS between rows whose `type` or `categories` disagree across the MOVIE/SERIES line is refused, and one uri whose answers disagree about its kind is an anomaly | all three |
| EF-2 | The digest cannot detect the mutation it claims to prove absent: `sum(size(a.hash))` is invariant at fixed hash length, counts cannot see a `SET`, and it runs only in dev (engine refuter 10) | **Fatal for the claim, not the architecture. Upheld.** The final design drops the word proof for any audit: the real argument is that `PluginOutput` is data and the applier is the only writer; the audit is a content hash over every source table (always on in dev and in the harness, sampled in production), paired with a control plugin in the suite that rewrites one `Answer.raw` and must be caught | minimal-core D2 and product-first D2 (both `count + max(seq)`). All three |
| EF-3 | `count-sum` plus view step 4 ships `01 R1`'s outcome: a positionally placed container episode becomes a SAME_AS episode handle, which the watch page turns into a playable url (engine refuter 11) | **Fatal for the rule pair. Upheld, and one table row fixes it** (a container episode may contribute a displayed number through a range and may only become a SAME_AS handle where a per-episode pair exists). `count-sum` is deleted rather than left off (MC-1) | see MC-1 |
| PF-1 | A dateless, retranslating, folding source can become a member on count agreement alone (an `ask` SAME_AS waved through by guard 7, or P4 rule 3), and member episodes then place buttons positionally (cases refuter, `01 R1`, `01 R7`, `01 case 11`) | **Upheld in part, overruled in part.** Rule 3 is the plugin's OWN count-only road with no answerer behind it and no measured arm: deleted. But membership of a count-exact catalogue season through an `ask` claim is shipped behaviour: the answerer's five rules (`03 section 4`) answer a RUN, the consumer claims SAME_AS (`similar-consumer.ts:255`, verified), and the residue is measured (`01 case 11`: 11 of 105). The final design keeps that road at parity, subject to every guard, and marks positional member rows `via: member` in the trace so the residue is visible rather than hidden. Demanding positive evidence beyond the answerer's rules would be a new refusal with no arm, which `01 I2`'s counterweight forbids | minimal-core I5 (`C == L` accepted as claimed), evidence-first `similar` judge: the same parity in both |
| PF-2 | Migration step 5 deletes `lendContainingSeason` with nothing to replace the fetch: `pickSimilarSeason`'s fold veto and `isRunAnswerFrom` (`similar.ts:125-129`, verified) refuse a containing season, so no Crunchyroll row for it ever enters the graph (cases refuter, `01 case 12`) | **Fatal as written, confirmed**: the lend is reached from `searchAndLinkMedia` on the media path (`crunchyroll/extractor.ts:531`, yielded at `:578`), not through `similarMedia`. Fixed by grafting the `containing` answer (evidence-first 5.7, minimal-core 4.4.2) and keeping the lend's `HAS_EPISODE {claimer:'cr'}` shape accepted as evidence until each answerer ships it (product-first 4.4) | evidence-first keeps the lend AND adds `containing` without saying what happens when both fire (its cases refuter, finding E); minimal-core rewires the answer but never says the media-path lend is removed. All three need the transitional rule |
| PF-3 | An undowngraded refusal (`disagreeing-ids`, `contested`) writes no usable edge, so both AoT parts lose MAL's row and the refused `mal:51535` becomes its own listing card (cases refuter, `01 case 43`, `01 I2`) | **Fatal as written. Upheld.** A refusal must keep a rendering path: the claim downgrades to `PART_OF {reason: 'contested' \| 'disagreeing-ids'}` from each claimant to the shared row, the shared row is hidden as attached, the anomaly reports the contest, and fields never cross (the exchange, C3 in minimal-core's words) | minimal-core I6 (refuses the loser by a hash tie-break, same data loss, arbitrary winner); evidence-first `Refusal {rule:'ids'}` (the first join stands, the second loses). All three lose a row on one side; the final design downgrades on both |
| PF-4 | `run()` must return the WHOLE desired output and the writer applies `DELETE current - desired`, while `delta` exists "so a scan can be scoped"; a scoped run retracts everything outside its scope every flush (engine refuter D1) | **Fatal as written, and minimal-core already holds the fix**: `reconcile(table, desired, scope)` diffs only inside a declared scope, and idempotency is stated as "same graph, same scope, same output" | evidence-first's applier ("retract the rows it minted about that subject, then emit") is scoped by construction and clear of it |

Two patterns fall out. First, six of the sixteen (MC-6, MC-7, MC-8, MC-9, EF-1, EF-2) are shared by all
three proposals: they are properties of the problem (an async engine under a 50 ms flush, retraction
against an address that only grows, a hook keyed on a type, a uri that is also a route, an audit that
cannot see a `SET`) and the final design has to answer each once. Second, every proposal re-opened a
count-only road to a play button (MC-1, EF-3, PF-1): the record's `01 R1` was about a title vote, and
each designer read it as narrowly as that, when the lesson is that nothing places a per-episode link
without per-episode evidence.

---

## 3. The verdict: product-first, with grafts

### Why product-first is the base

1. **It is the only one that already has the two shapes the other two are missing.** One writer with a
   guard list and a downgrade (the RUN versus CONTAINER exchange stated once, `02 section 3` items 4
   and 5) is what `01 cases 2 and 7` ask for; evidence-first spreads it across `claims`, `vetoes` and
   `membership`, minimal-core has it as identity rules I1 to I8 with a JS union-find. And the `Answer`
   log with `fieldSeq` and `supports` chains is the only mechanism proposed that turns a production
   bug into a replayable fixture (`02 section 5.2` says this is impossible today).
2. **Its six plugins are the brief's plugins.** Direct edge merging, title matching, season and episode
   range matching, plus profile, containment and aggregate. Evidence-first's fifteen are a finer
   decomposition with a fifteen-step round, back edges and a repeat step; the owner asked for
   "simpler and easier to reason about".
3. **Its fatals are cheap once grafted.** PF-2 is a schema addition the other two already carry; PF-3
   is a downgrade rule; PF-4 is minimal-core's reconcile; PF-1 is one deleted rule plus a parity
   statement. Evidence-first's fatals are also cheap, but its serious list is longer and deeper in the
   queries (scope null, key collision, `title-match` unable to mint the cross-scope `PART_OF` its own
   walkthrough depends on, `episode-number` regressing `01 case 19`, rule B dead for Crunchyroll).
4. **Its read path and walkthroughs are closest to today's contracts.** Resolve then members then slots
   reproduces `findMediaForPage`, `preferAttachedRun` and the SAME_AS-only episode walk, with the slot
   read structurally unable to receive a container's uris (`04 section 2.1`'s "most dangerous read").

What product-first loses without the grafts: containment as a first-class evidence object (the
`containing` answer), the queryability of vetoes (`skipped:absent`), the invariant queries, the
exchange-rate rigs, and the write contract's scope.

### The grafts

Each is a concrete change to the base, naming the section of the final document it lands in.

| # | graft | from | change to the base |
| --- | --- | --- | --- |
| G1 | The `containing` answer | evidence-first 5.7, minimal-core 4.4.2 | Section 4 and 5.4 P2: `similarMedia` returns `{ media?, containing? }`; `containing` is the ONE season that holds the asked run without being it (answerers with a premiere: the season whose episode span covers the run's start and which is longer; answerers with titles only: `pickSimilarSeason` rule 2's bar, `MIN_EPISODE_TITLE_MATCHES = 3` over non-generic titles, with the fold's signature read as coverage BELOW `EPISODE_TITLE_COVERAGE = 0.6` and a count above the run's). A `containing` row lands as an answer plus a `CLAIMS {kind:'PART_OF', provenance:'ask'}` from the run's key member to the season; it never claims sameness. Until an answerer ships it, the media-path lend (`HAS_EPISODE {claimer:'cr'}` onto a foreign uri) stays accepted as `plugin:range` evidence and never as a slot (product-first 4.4) |
| G2 | The scoped reconcile | minimal-core 5.1 | Section 5.1 and 5.2: `run()` returns `{ scope, output }` where `scope` names the keys it recomputed (clusters, pairs, uris); the writer diffs only inside that scope; `delta.full` means the whole table; an edge whose endpoints straddle the boundary belongs to the scope that named either endpoint. Idempotency is "same graph, same scope, same output", and `runPlugin` asserts it that way |
| G3 | Content-hashed answers | minimal-core 2.1 and 4.3 | Section 4.2: `Answer` is keyed by a hash over `(origin, kind, uri, canonical JSON)`; a byte-identical re-fetch writes no row, moves no `seq`, emits nothing. The batch reads back existing answer keys and claim keys in one query so `changed` is computable (product-first B3). A pass runs only when a new row, a new claim, a new `HAS_EPISODE`, or a change in a projected column landed; a change confined to unprojected `raw` fields updates the row and emits `row:changed {uris}` only, which is what wakes `ctx.listenForMediaChanges` and the seven `waitForMedia` callers (`04 section 1.8`) |
| G4 | The split veto and the end of rule 3 | evidence-first 5.3 `split`; the record `01 case 10` | Section 5.2 guard 7: the LONGER direction downgrades always (zero tolerance, `01 I6`); the SHORTER direction downgrades only when the run is FINISHED (a short fetched list on a RELEASING run is not evidence, `01 case 23`; the Elusive Samurai row stays a member as shipped, `01 case 13`). Section 5.4 P4: rule 3 (count-exact SAME_AS) is deleted, and the "every numbered episode paired and shorter, therefore SAME_AS" upgrade is deleted (it is the Fullmetal Alchemist shape `01 case 10` says only exactness may admit). A dated season proven by pairs stays `PART_OF` with its pairs; its episodes reach the slots through the pairs, which is everything the user sees |
| G5 | Silent gates recorded on minted links | evidence-first 3.3 | Section 5.4 P3 emits: every `LINK` the title plugin writes, accepted or refused, carries `gates: {format, season, date, companion}` each `passed \| refused \| silent`, so `01 case 6`'s four silent short-circuits are countable on accepted pairs as well as refused ones |
| G6 | Contested over the graph, downgraded | evidence-first 5.4 step 1; product-first guard 5 | Section 5.2 guards 4 and 5: `disagreeing-ids` and `contested` are evaluated against the graph as it stands (an active SAME_AS into a target from a component that disagrees with a new claimant retracts BOTH), never within one pass; both refusals downgrade to `PART_OF {reason}` toward the shared row, the shared row's singleton cluster is hidden as attached, and `Cluster.anomalies` names the contest |
| G7 | The listing hide rule | minimal-core 5.9 `attached`; evidence-first 6.1 `FOLD` | Section 6.1: a cluster of any scope is hidden from a listing when every member is a `PART_OF` target or an `INCLUDES` source of some run cluster in the store AND no member is from a per-run metadata origin (mal, anilist, kitsu, anizip, offline, simkl anime rows: `03 section 1`), unless it is the requested uri. Store-wide (product-first C9), so a folded Netflix season, a JustWatch season, a lent Crunchyroll season and a contested `mal:` row all hide behind the runs they hold, and a live-action container with no run keeps its card (`01 case 49`) |
| G8 | The invariant queries | evidence-first 3.4 | Section 5.3 and 9.3: after every pass in dev and after every harness run, four queries must return 0: an `INCLUDES` inside one cluster, a `PART_OF` inside one cluster, a SAME_AS link across scopes, a media with two `MEMBER_OF` rows. Failure is an anomaly with the cluster ids |
| G9 | The exchange-rate rigs | evidence-first 9.4 | Section 9.3: `scripts/measure-*.probe.ts` are rewired to seed the graph and run the plugin under test, so the arms of `02 section 2.10` (83 refused for 81 lost at 45 days; 49 of 84 with 2 lost for the companion pair; the refused silence rule) run against the new code before any threshold or any new refusal ships |
| G10 | Typed evidence declarations | evidence-first 2.2 and 2.3; minimal-core 4.5 | Section 2 and 5.4 P0: optional additive fields on the source schema (`startDatePrecision`, `startDateDerivation`, `episodeCountKind`, `episodeNumberSpace`, `MediaTitle.class`, `MediaHandle.provenance`), read by the profile plugin when present and replaced by its per-origin default table when absent; `PluginSourceMeta` gains the same declarations (`countKind`, `folding`, `retranslates`, `showLevel`) so a remote source declares rather than inherits a guess, with `listLength` the default for an undeclared origin |
| G11 | Exact title agreement as a join | evidence-first 5.6 scan A | Section 2.2 and 5.4 P0 and P3: `titleKeys` becomes a JSON array of `{key, score, language, class}` (the shape `01 case 30`'s selection needs), and the profile plugin also emits plugin-owned `TitleKey` nodes and `HAS_KEY` edges so "which clusters share a normalized MAIN title in this year bucket" is one query; the fuzzy scan reads the six capped keys per cluster in title-ascending order within a score tier |
| G12 | Claim precedence classes | minimal-core 5.5 I7 | Section 5.2: the writer evaluates SAME_AS proposals per pass in a fixed order: `source` between two per-run origins, `source` naming a season-scoped catalogue id, `ask`, `seed`, title links, `address` with the claimer as an endpoint; within a class by `(from, to)`. Determinism without a hash tie-break |
| G13 | Materialized aggregates | minimal-core 5.9 and 6; evidence-first 5.12 | Section 5.4 P5 and 6: `plugin:aggregate` writes `Cluster.card` (the listing's fields: `_id`, `uri`, titles, covers, popularity, `episodeCount`, and every field `applyMediaFilters`, `searchRelevance` and the sorts read) and `Cluster.media` (the full aggregate with `provenance`) per dirty cluster in one `UNWIND` batch, and `Slot.episode` likewise; every read is a lookup, `raw` is read only by the trace panel and the replay |
| G14 | Distinct episodes beside the stated count | evidence-first 5.14 and its refuter's fix for `01 case 16` | Section 5.4 P0: the profile carries `countStated` (the source's own figure) and `countDistinct` (`count(DISTINCT HAS_EPISODE targets)`); guards 7 and 8 and `INCLUDES.total` read `countDistinct` when a list exists; disagreement is an anomaly |
| G15 | A source `INCLUDES` inside its own id space | evidence-first 3.1 | Section 3.1 (owner decision 3): `MediaHandleRelation` gains `INCLUDES`, accepted from a source only from a show row to season rows of the SAME origin whose ids extend the show's (`idParent`), else refused with reason `foreign-includes`; that is how unogs's show answer carries its five seasons for `01 case 10` and how a container page lists seasons |
| G16 | The ask log | evidence-first 2.2 `Ask` | Section 7.3 and 7.5: the similar consumer writes one `Ask` row per question (cluster, origin, question hash, outcome: answered, refused, declined with `SimilarOutcome`'s reason), so "why is Netflix missing here" distinguishes never asked from asked and refused. The loop itself stays `askUnasked` keyed on origins (`media/index.ts:58-65`) plus the consumer's caps (`04 section 1.6`) |
| G17 | Scope flips retract | evidence-first finding 13 (its engine refuter) applied to product-first's ratchet-as-view | Section 5.2 guard 2: an active SAME_AS whose endpoints' effective scopes now differ is retracted and downgraded on the next pass; the `justwatch` before `crunchyroll` race of `02 section 2.4` is the fixture |
| G18 | Anchors for every named uri, singleton clusters for every scoped row | minimal-core 2.1 and 5.5; product-first C1 | Section 4.3 and 5.4 P5: a placeholder is stored on first naming (already in product-first) AND every row with an effective scope, placeholders included, sits in a singleton cluster, so `ATTACHED_TO` reaches a PART_OF-only container and its badge renders from the best claimer's `node.url`; a placeholder cluster is never a card and contributes no field |

---

## 4. Decisions only the owner can make

Each changes the schema or the plugin contract. Everything else is decided in section 5.

1. **What an address-bar handle may assert** (`01 case 8`, open problem 2; all three proposals raise
   it). *Recommend*: an `address` claim is consumed only when the claimer's own row is one endpoint (a
   source saying "I resolved myself out of this cluster": Crunchyroll's season row to `anilist:X`), and
   is subject to every guard like any claim; an `address` claim relating two rows both foreign to the
   claimer (a bookmark rebroadcast between siblings) is a pointer that routes re-asks and never enters
   the closure. *Why*: `buildHandlesFromUri` is the ONLY attach path for crunchyroll, unogs, appletv
   and justwatch rows resolved through an aggregated uri (`03`), so a pure pointer rule turns every
   such row into a singleton and, under G7, a hidden or orphan card; the stale show-level `cr:` of
   `01 case 6` is already CONTAINER and rides `PART_OF`, and two old season links in one uri are two
   ids of one origin that guard 4 refuses. *Cost of pure pointer*: an unpriced number of duplicate
   season cards. *Cost of full assertion*: the re-injection path stays open, bounded only by the scope
   derivation and guard 4.
2. **`similarMedia` returns `{ media, containing }`** (G1). *Recommend* yes. *Why*: it is the fetch
   that `01 case 12`'s measured win needs once the media-path lend goes, and it lets unogs and
   JustWatch say "this season holds your run" without lying about sameness (`01 case 9`). *Cost
   otherwise*: the lend hack stays permanently in one extractor, and the fold is never visible for
   Netflix or JustWatch.
3. **`MediaHandleRelation` gains `INCLUDES`** as an output value (a run's page lists the seasons that
   hold it, a run split across seasons lists them, a container page lists its seasons) and as a source
   claim fenced to one id space (G15). *Recommend* both. *Why*: without the output value the split
   direction (`01 case 10`) has no rendering; without the fenced source claim unogs's seasons never
   enter the graph for a refused ask. *Cost otherwise*: the split renders as nothing, and the
   container page cannot list seasons. It is a client-visible enum change; the badge reader takes
   "SAME_AS first then anything" and playback takes SAME_AS only (`04 section 2.3`), so no button
   can come from it.
4. **The optional evidence fields on the source schema and on `PluginSourceMeta`** (G10). *Recommend*
   yes, all optional, kitsu and jikan first for `startDatePrecision` (`01 case 34`'s measured 30 day
   error). *Why*: every per-origin table inside the profile plugin is `01 case 44`'s mistake in a new
   place (a constant standing in for a per-claim property), and `01 case 33` priced the field at
   "touching every extractor", which optional fields avoid. *Cost otherwise*: the per-origin tables
   stay hard-coded at parity and the genuine January 1 premiere stays invisible (0.52%).
5. **`provenance`, `anomalies` and `kind` as public fields on the aggregated `Media`, and the
   `?trace=1` panel in production builds.** *Recommend* yes. *Why*: they are the debugging story the
   brief asks for ("easy to trace changes"); each is one field on a type the app already keys, plus a
   `MediaFieldProvenance` entry in `src/urql-keys.ts` keyed `null` like every other embedded type
   (`04 section 5.5`). *Cost otherwise*: the trace stays a dev-only osra method and a bug report stays
   a screenshot.
6. **Merge plugins are first-party only, or a third-party surface.** *Recommend* first-party only for
   this design. *Why*: the isolation argument rests on the applier being the only writer and on the
   harness hashing source tables; a third-party plugin would need the query facade to be a hard
   boundary and its own measurement, and remote sources already have exactly one function and never
   the real context (`04 section 4.4`). *Cost otherwise*: the facade becomes a security surface with
   no measured behaviour behind it.

Decided here, so the owner need not: refused links live in `LINK` with `status` (one query away from
the active ones); the diacritic fold stays off until the 150-season arm runs (`01 I8`); `punct-join`
variants are not shipped (they serve nyaa's index, not stub's merging, `01 case 31`); anizip's row stays
unscored for scalars (parity, `01 open problem 8`) while the count vote reads classes so its exact count
wins where a DECLARED class should; the engine loads behind the flag until step 3 rather than for every
cold load (a 22.1 MB download, `00`); the two-witness bar applies to the WINDOW (trimming) and not to a
date-proven pair (a pair lands only on an existing member row inside `1..runLength`, so it can add no
row, `01 I9`); no positional loan without a pair (MC-3); `count-sum` and C4 do not exist; a `Slot` is
keyed `<clusterId>#<number>` for numbered rows and `<clusterId>#s:<lowest uri>` for specials, accepting
that a renumbering re-keys a row (today's modal keys rows on the number already, `04 section 2.3`).

---

## 5. The synthesis brief

Write the final document in the eleven-section format, from product-first's text as the base, with
the changes below. Every threshold is the record's number with its citation, or is marked NEW with the
case that needs it and the arm that must run before it moves. No em dash or en dash anywhere.

### Section 1. Principles

Keep product-first's five read guarantees G1 to G5 and its eight principles, with three edits.
Principle 2 ("nothing is irreversible") becomes: every merge is a deletable edge recomputed from
evidence, AND an active SAME_AS is sticky, retracted only by a guard whose evidence changed (the
termination argument, MC-7). Principle 6 becomes the sentence that closes MC-1, EF-3 and PF-1: a range
labels a container and bounds a search; a per-episode SAME_AS handle exists only where a per-episode
pair with evidence exists; a member's own positional rows are `via: member` and say so. Add principle
9 from evidence-first: absent, zero and unknown are three values on every quantity a rule reads, and a
gate whose premise is absent records `silent`, never `passed` (`01 cases 6, 23, 26`).

### Section 2. Graph schema

Product-first 2.1 and 2.2 with these changes:

- `Answer` is keyed by a content hash (G3): `key STRING PRIMARY KEY` over `(origin, kind, uri,
  canonical JSON)`, plus `seq`, `operation`, `selection` taken from `info` in the hook (product-first
  B7), `raw`. A byte-identical re-fetch is a no-op.
- `Media` keeps `uri` as the key, `origin`, `id`, `owned`, `scope` (the owner's own last word), `raw`
  (the owner's field-merged row, a CORE PROJECTION rebuildable from the `Answer` log by the stated
  rule, never a source row on its own), the verbatim typed projections, `fieldSeq`. State that the
  `Answer` log is the only source of truth and `raw` is the ingest's cache of it.
- `Episode` gains nothing; `numberSpace` lives on the profile (G10).
- No `DATE` or `TIMESTAMP` column anywhere a plugin writes: `startDay` and `EpisodeProfile.day` are
  `INT64` UTC day numbers (`floor(ms / 86400000)`, `consensus.ts:85-88`), which keeps every window
  inside `00`'s exercised dialect (product-first A7, evidence-first 4).
- `MediaProfile.titleKeys` is JSON `[{key, score, language, class}]` (G11); `MediaProfile` gains
  `countStated`, `countDistinct` (G14), `numberSpace` on `EpisodeProfile`, and `dateSubject` (`run` or
  `container`, from the row's effective scope, so year buckets and day sets are built from RUN-scoped
  members only: `01 case 3`).
- Plugin-owned `TitleKey(key STRING PRIMARY KEY)` and `HAS_KEY(FROM Media TO TitleKey, by, score,
  language, class)` (G11).
- `Cluster` gains `card JSON`, `media JSON`, `anomalyCount INT64` (product-first A6), `aliases` both
  ways (MC-8). `Slot` gains `episode JSON` (G13).
- `LINK` gains `gates JSON` (G5). Declare every `FROM/TO` pair at `CREATE` time (`00`: one rel table
  may span several pairs; an `ALTER` is not among the exercised facts).
- `Ask(key STRING PRIMARY KEY, clusterId, origin, showId, questionHash, outcome, reason, seq)` (G16).
- `Origin` rows are seeded from the extractor definitions at boot so imdb has a row (evidence-first
  2.2; `01 case 42`).
- Keep the "who writes what" table (product-first 2.3) and add: the read path writes nothing; the
  audit is a content hash over every source table (EF-2).

### Section 3. The edge model

Product-first 3.1 to 3.5 with:

- `INCLUDES` gains the fenced source form (G15, owner decision 3) and stays whole-to-part with a
  range when minted by `plugin:range`; containment established with no correspondence is `PART_OF`
  with a `reason` that names the mismatch and `evidence: {theirs, ours}` (which is evidence-first's
  rangeless `INCLUDES` under the base's vocabulary; state the equivalence so a reader of either
  proposal finds it).
- The provenance table 3.2 gains `gates` on plugin links and `via` on `FILLS`.
- 3.3's `address` rule reads as owner decision 1's recommendation, marked as the owner's call.
- 3.5: keep the three walk queries, name the pattern in every filtered variable-length path exactly as
  `00` verified (`-[e:LINK*0..8 (r, _ | WHERE ...)]-`), and add the four invariant queries (G8) as the
  guard's post-pass check. The owner's rule holds three ways: guard 6 at proposal time, the post-pass
  invariant with a declared precedence (a `PART_OF` or `INCLUDES` between members of one cluster
  retracts the weaker SAME_AS, product-first D3), and the closure filter.

### Section 4. Ingest

Product-first 4.1 to 4.4 with:

- 4.1: the hook keys on position as well as type (MC-9): only a result whose parent is the
  subscription root or `MediaPage` is an answer; `MediaHandle.node` and a handle node's `episodes`
  never reach the inserter. Test: a nested node must NOT produce an `Answer`; control: a top-level
  row must.
- 4.2: the batch dedupes rows in JS before the `UNWIND` (evidence-first 18), reads back existing
  answer keys and claim keys in the same round trip as the rows (G3), and computes `changed` from
  them; a malformed row is dropped and recorded, never allowed to reject the shared batch
  (`04 section 4.3`'s 2026-09-05 incident); the ingest is wrapped so a throw leaves the old store's
  result untouched during the dual-write steps. `CLAIMS` uses `NOT EXISTS` plus `CREATE` (the
  exercised form) and an `ON MATCH` field merge for `node` keyed on `answerSeq` (product-first A8).
  Budget the unwrapped row count, not the loader's 250 (product-first B2).
- 4.3: the owner rule, the placeholder rule and the field merge as written; add that `Media.scope`
  is the owner's last word and the effective scope is the profile's.
- 4.4: the transitional lend rule (G1).
- New 4.5: events. `graph:changed {seq, uris}` per commit (wakes `Subscription.media` for its own
  cluster and `ctx.listenForMediaChanges`), `row:changed {uris}` for a raw-only change (G3),
  `view:changed {clusters}` after a pass (wakes `mediaPage` with the 100 ms debounce), `origin:changed
  {ids}` only when a row changed. The seven `waitForMedia` callers are woken by the first two, which
  is the check `04 section 3` demands.
- New 4.6: the source-side changes the ingest needs, listed as migration items: the seven
  `?? episodes.length` sites read `episodeCount` only (MC-5, `01 case 23`); `similarMedia` answerers
  gain `containing` one at a time, Crunchyroll first; the four extractors that fetch and drop episode
  air dates emit them (tvmaze, trakt, simkl, tvdb, `03 section 3`).

### Section 5. Plugins

Product-first 5.1 to 5.4 with:

- 5.1: `PluginOutput` gains `scope` (G2). The contract's idempotency sentence is "same graph, same
  scope, same output". `PluginContext.titleSimilarity` is documented as `src/sources/utils.ts:251-260`
  (normalized by the longer string in both directions, weaker direction taken), never the nyaa
  matcher's (`01 case 32`).
- 5.2: the guards in this order: `unknown-scope`, `cross-scope` (re-evaluated for active links every
  pass, G17), `address-only` (owner decision 1), `disagreeing-ids` and `contested` over the graph with
  the downgrade (G6), `contained`, `count-mismatch` with the FINISHED gate for the shorter direction
  (G4), `no-length`, `kind-mismatch` (EF-1: a SAME_AS across the MOVIE/SERIES line is refused). Every
  refusal that is not `unknown-scope` downgrades to `PART_OF {reason}` so a badge and an offer survive
  and no refusal creates a card (PF-3). Proposals are evaluated in the precedence classes of G12. The
  sticky rule (MC-7): an active SAME_AS is re-checked against the guards each pass and retracted only
  when a guard now fails; profile drift never retracts.
- 5.3: one pass per flush, coalesced; the fixed point is detected by a zero delta or a repeated state
  hash, reported as an anomaly at the cap of four; a throwing plugin keeps its previous output, the
  pass continues, `view:changed` fires for what ran, and the failure is an anomaly (product-first
  D4, evidence-first 8). Isolation: the applier is the only writer; the audit is a content hash over
  every source table for rows with `seq <= passStart`, always on in dev and in the harness, sampled in
  production, with a control plugin that must be caught (MC-6, EF-2). Every plugin scan filters on
  `seq > since` or on `delta`, never on `list_contains` over a thousands-long list (evidence-first 20).
- 5.4 P0 profile: as written plus `countStated`/`countDistinct`, `numberSpace`, `dateSubject`, the
  per-title `{key, score, language, class}` and `TitleKey` nodes, the declared fields of G10 read
  first and the per-origin table as the default, an undeclared origin defaulting to `listLength`.
  `datePrecision` is derived from the parsed value (UTC day-of-month 1 from a coercing origin, or
  from any origin until it declares precision), never from the string shape (evidence-first 33).
- 5.4 P1 direct: as written; a `containing` ask claim lands as `PART_OF` reason `containing` with the
  season's count and the run's length in `evidence`.
- 5.4 P2 containment: the span scan anchored on an existing `PART_OF` or `ATTACHED_TO` edge, never a
  cartesian product (product-first A9); the season's start date optional, the span taken from its
  own episode days (evidence-first 12); `preferredRun` ordered by the earliest parseable start day
  then by `key`.
- 5.4 P3 title: as written, reading the six keys per cluster in title-ascending order within a score
  tier from the JSON `titleKeys`, the exact scan through `TitleKey` (G11), the year buckets from
  RUN-scoped members only, `gates` recorded on every link (G5), the LRU decision cache.
- 5.4 P4 range: rule 1 (dates) and rule 2 (titles, refused outright on `retranslates`) as written;
  rule 3 deleted; the media-level verdict table reduced to two rows: pairs found (any count) mint the
  per-episode links and an `INCLUDES` with the hull, `aligned/total` and `contiguous` when the
  container is longer, and the existing `PART_OF` stays in every case (G4). The alignment among
  members (Crunchyroll numbering 13..20 inside a cluster, `01 case 13`) emits pairs and `FILLS.number`
  exactly as 8.2 walks it.
- 5.4 P5 aggregate: as written (classes then tiers, `runLengthWitnesses` counted across every member
  whose count equals the chosen value, which is today's `backing`, `02 section 2.9`; the spared sets
  `reference` and `equals`; slots from member episodes and proven pairs only; slots only for RUN
  clusters, product-first C2) plus the materialized `card`, `media` and `episode` JSON per dirty
  cluster in one `UNWIND` batch (G13), the singleton clusters for every scoped row including
  placeholders (G18), and the published-identity rule (MC-8): the cluster id is carried by the
  fragment holding the previous `key`, aliases both ways, the aggregated uri append-only per cluster
  id.
- New 5.5 anomalies: the rules of product-first P5 plus evidence-first's `over-length`,
  `count-disagrees` (G14), `kind-disagrees` (EF-1), the four invariant queries (G8), and a
  `constant-id` rule counting distinct CLAIMANTS of one target before membership (`01 case 40`, its
  refuter's fix). Every rule ships with a control fixture that must report zero.

### Section 6. The read path

Product-first 6.1 to 6.6 with:

- 6.1: two statements (with uris, without), the node bound (`MATCH (m:Media)-[:MEMBER_OF]->(c)
  WHERE m.uri IN $uris`), aggregations keyed on `c.id`, returning `Cluster.card` only (G13); the hide
  clause is G7; `applyMediaFilters`, `searchRelevance` at 0.7 and the sorts unchanged in JS over the
  cards.
- 6.2: resolve then `Cluster.media`; an `_id` resolves through `aliases`, and a resolve that lands on
  a cluster not holding the requested member falls through to the member uri (MC-8); a container
  follows `preferredRun`; a requested uri is never hidden.
- 6.3: `aggregateFields` stays the one place a field is chosen, executed by `plugin:aggregate` at
  write time, with `provenance` recorded; the singleton path is gone, and the migration names what
  that changes (a one-member card's `uri`, `origin` and `url`, product-first C10, with
  `asAggregatedUri` accepting both spellings).
- 6.4: `Slot.episode` lookup; `INCLUDES` and `PART_OF` handles never yield a button, pinned in
  `episode-origins.test.ts` (product-first C11).
- 6.5: the rendering table as written, plus the Netflix season row opened directly (a FOLD page: its
  own rows in its own numbering, "holds: cour 1, cour 2" as links) and the contested `mal:` row
  (hidden, badge on both parts).
- 6.6: the four events of section 4.5.

### Section 7. The trace loop

Product-first 7.1 to 7.5 with: `askUnasked` keyed on origins as the code does (`media/index.ts:58-65`),
the `Ask` log (G16), the similar consumer reading `ATTACHED_TO` and the run's own slots (never a lent
row) for its evidence, the trace panel's path query replaced by a walk over `LINK.supports` (product-first
A5), and `anomalyCount` in place of `size()` over JSON (A6). Keep 7.4 verbatim: it is the owner's
prohibition walked end to end.

### Section 8. Walkthroughs

Product-first 8.1 to 8.5, corrected: in 8.1 the Crunchyroll season arrives through the `containing`
answer (or the transitional lend) and its badge reads "holds this run, episodes 1 to 11"; S2 part 1's
`runLength` is 13 on one witness with 12 slots and `provenance {witnesses: 1}`; Netflix's outcome is
traced through the `Ask` row (`declined: fold`). In 8.2 the witness count is four (every member whose
count equals 12) and the Crunchyroll row is a member through the ask claim with guard 7 silent on the
shorter direction because the run is RELEASING (G4); no `aligned` upgrade is needed and none churns.
In 8.4 the seasons enter through unogs's fenced `INCLUDES` claims (G15) and render as links only. Add
the `mal:51535` AoT case as 8.6 (G6): two clusters, two `PART_OF {reason:'contested'}` badges, one
hidden singleton, one anomaly. Every uri and number is the record's; where the record carries none, say
so rather than invent.

### Section 9. What is kept, what goes

Product-first 9.1 with the export walking `LINK {kind:'SAME_AS', status:'active', by:'plugin:direct'}`
whose `supports` name a `source` or `ask` claim (never `seed`, never `address`, never a title link),
with the cluster drop rule and the total ordering (product-first C3; `01 case 47`). 9.2 with
`store/consensus.test.ts` moved to REWRITE, case for case, before the old functions are deleted
(every refuter caught this); `tieredConsensus` and `runLength` genuinely verbatim. 9.3 as written plus
G8, G9, the corpus of real payloads (the 14 merge fixtures, `04 section 5.1`) as the arm for
multi-answer and nested-node behaviour, and the `04 section 5.5` suites named with a case for a
shrinking cluster. Keep "every 01 case a fixture, every R a negative fixture, every refusal with a
control".

### Section 10. Migration

Product-first's seven steps with: step 1 split in two (engine, schema, `Answer` log and the audit
first; the `Media`, `Episode`, `CLAIMS` ingest second) and priced honestly; the engine behind the flag
until step 3; the tee measured on a real `MEDIA_PAGE` fan-out before it ships to everyone; step 3 moves
`plugin:title` in with `media` so the listing and the detail page never disagree about membership
(product-first F4); the `address` stamp its own flagged step before any deletion (F6); step 6 flips,
bakes, then deletes in a separate commit (evidence-first 28); the consensus function moves sequenced
into the step that lands `plugin:range`; step 7 as written plus the `?? episodes.length` source fix.

### Section 11. Risks and decisions

Product-first's risk table with the unmeasured numbers named as step-2 measurements against the old
store on the same replay (pass cost under a 24-source flush, bytes per listing read, the tee's cost),
and the decisions replaced by section 4 of this judgement: six for the owner, the rest decided and
listed as decided. Close with the fifteen invariants table, each row naming the mechanism that carries
it in the final design; none is dropped, and I1 is restated as "reversible on contradiction, monotone
otherwise".
