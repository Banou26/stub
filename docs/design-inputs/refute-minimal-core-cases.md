# Refutation of `proposal-minimal-core.md` through the edge case lens

Every numbered case of `01`, every refutation `R1` to `R26`, every invariant `I1` to `I15`, walked
against the proposal as written. Verdicts: **HOLDS** (the stated rule and the real rows produce the
right edge), **FIXABLE** (wrong or missing, repairable inside the design), **FATAL** (the design
cannot satisfy it without changing a principle or a worked example).

Two conventions used throughout:

- "the proposal's N" cites the proposal's own section numbers (2.1, 5.5 I5, 5.9, 8.1 and so on).
- A verdict is not granted for "a plugin would catch that". Each row names the table, the edge, the
  rule and the rows.

---

## Part 1. The 51 cases

| case | verdict | how, or why not | what would fix it |
| --- | --- | --- | --- |
| 1. Kitsu's one Crunchyroll series url on three seasons (`01 case 1`) | HOLDS | Three `CLAIMS {relation:'PART_OF', targetScope:'CONTAINER', provenance:'SOURCE'}` from three kitsu payloads to one `cr:G24H1N3MP` anchor (proposal 2.1, 8.5). Identity I4 derives three `LINKS PART_OF`, never a membership, and `reconcile` can retract them (proposal 5.1). The (origin, segment) allowlist stays source-side (`03 kitsu`), so nothing about the shipped fix is re-litigated. | nothing |
| 2. A show-level catalogue id bypassing every veto because it is an id (`01 case 2`) | HOLDS | The strongest part of the design. The identity scan reads both endpoints of every claim (proposal 5.5 candidate scan: `(p)-[:DESCRIBES]->(s)` and `(p)-[c:CLAIMS]->(t)`), and `plugin:title`'s output re-enters the same rule set as `LINKS SAME_AS` (proposal 5.7, 5.5 scan `UNION ALL`). So "a guard written once applies to both" and "reachable from the direction the claim arrives in" are both structural rather than a Set lookup on the handle side (`02 section 2.1` `SHOW_LEVEL_ORIGINS`). | nothing |
| 3. A SHOW's date on a SEASON row welding through the year bucket (`01 case 3`) | HOLDS, with a gap | `years` and `days` are computed per payload (proposal 5.7 profile), and the offending rows were fixed source-side (`01 case 3`: tvmaze derives the season premiere, tmdb asserts no date). The case's second half, "a cluster's derived attributes must be attributable to the member that supplied them", is only met for accepted pairs: `LINKS.evidence` carries `{year, days}` but not which payload supplied each year (proposal 5.7 emits). | put the supplying payload key beside each year and day in the profile's evidence |
| 4. A borrowed id makes the borrower's own origin the common case (`01 case 4`) | HOLDS | Source-side (`offline`'s `getMedia` early return). The anchor is keyed on uri alone with `origin` as a plain column (proposal 2.1), so identity is separable from the asserting origin, which is the case's "must express". Nothing in the proposal reintroduces the early return. | nothing |
| 5. Two ids of one origin, prefix extension the only exemption (`01 case 5`) | HOLDS | Identity I6 uses `extendsId` one way (proposal 5.5 I6, `01 R18`). `mostSpecific` is retired honestly: `ag:(...)` is built from routable MEMBER uris only (proposal 5.9 `uri`), and membership holds RUN-scoped ids only (I4), so the bare `cr:` that sorted first in `extractAggregatedUriOrigin` is not in the uri a source is asked about. | nothing; see case 11 for I6's arbitration |
| 6. A bare container id in season 1's cluster bridging it to season 3 (`01 case 6`) | HOLDS | The bare `cr:G24H1N3MP` and bare `nf:80987039` are CONTAINER by their own rows (`03 crunchyroll`, `03 unogs` `titleScope`), so a title match against a run cluster is RUN x CONTAINER and rides `LINKS PART_OF` (proposal 5.7 emits). The bookmark re-assertion is `provenance:'URI'` and never admits alone (I3). Note the record never proved the chain (`01 part 4 problem 1`), so this is a structural argument, not a reproduction. The case's own "must express" (an absent-premise veto must be distinguishable from a pass) is met only on accepted pairs, since a refused title pair writes no row at all. | write a `VERDICT`-shaped row for a refused title pair carrying which vetoes were evaluable, so four silent short-circuits are countable |
| 7. A second writer into the union-find with none of the guards (`01 case 7`) | HOLDS | `MEMBER_OF` has exactly one producer (`plugin:identity`, proposal 2.3 and 5.1 `produces`), and the runner refuses a write to a table a plugin did not declare. "The number of writers into the identity structure is one by construction" is delivered. | nothing |
| 8. An aggregated uri is user input and identity is rebuilt from it (`01 case 8`) | HOLDS | `provenance:'URI'` on `buildHandlesFromUri`'s handles (proposal 3.3, 4.5), and I3 never admits a URI claim on its own. This closes `01 part 4 problem 2` as a rule. The owner's call is correctly surfaced (proposal 11.2). | nothing |
| 9. One streaming season CONTAINS two catalogue cours, the fold (`01 case 9`) | **FATAL** | The date half works: `plugin:range` mints `INCLUDES` segments from shared air days for Crunchyroll (proposal 5.8, 8.1). The count half, rule C4 (proposal 5.6), does not. Two independent breaks. (a) **It cannot fire as written**: C4 requires "a container cluster member that is a RUN-scoped season row of a folding origin", and identity I4 never merges a RUN-scoped row into a CONTAINER cluster, so that set is empty by construction. (b) **Under the only coherent reading (a RUN-scoped season row that is `PART_OF` the same container), the arithmetic in the proposal's own walkthrough is wrong.** With the record's cours 11, 12, 12, 12, 14 (`01 group B`), the contiguous pair sums are 23, 24, 24, 26. So `jw:222366-<s2>` = 24 matches **two** windows (cours 2+3 and cours 3+4), and C4's own "exactly one such window" must refuse it, yet proposal 8.1 asserts it INCLUDES S2 part 1 and part 2. Relax the rule to admit the best window and Netflix season 1 = 24 gets the same two candidates, i.e. a one-in-two chance of an `INCLUDES` from Netflix season 1 to cours 2 and 3, which proposal 5.9 renders as a `PART_OF` handle with a playable Netflix url on both cour pages. That is `01 I2`'s lie, minted from a count and nothing else. The only sums that are unique across those five cours are 23 and 26, so the rule is simultaneously inert (it delivers one edge) and unsafe (the one relaxation that makes it useful makes it wrong). | delete C4. Count-partition is not evidence: it is `R20`'s offset composition wearing a different hat. If a count-exact fold must exist, require a second independent axis on at least one boundary (a shared air day at the seam, or an episode title match on the first episode of each segment) before any edge is minted, and never render a `COUNT` range as a handle |
| 10. One catalogue run SPLIT across several streaming seasons (`01 case 10`) | FIXABLE | Identity I5's `C < L` branch demotes a shorter catalogue season to `PART_OF` (proposal 5.5 I5), which does close BAKI (13 against 26 admitted today through `similar.ts` rule 4). But the rule is applied to **every** folding-origin row whose count is below the agreed length, including a Crunchyroll season that is simply still airing: `03 crunchyroll` sets `episodeCount` to the length of the fetched episode list, so in week 5 of a 12 episode run C is 5 and the row is demoted. Demotion is not free here: proposal 5.6 C3 says nothing crosses a `PART_OF` edge, so the page loses Crunchyroll's cover, banner and description (score 0.5, above kitsu's 0.3, `03 appendix`) for the whole broadcast season, and the media badge changes to a container link. The proposal admits the badge change (11.3) and not the field loss. The distinction the rule needs, declared count against fetched list length, is not on the wire (see case 22). | gate the `C < L` demotion on a DECLARED count only, and treat a fetched list shorter than the agreed length as no evidence at all (`01 case 23`: absent is not zero). Declared/derived must be a schema field, not a per-origin constant inside the plugin |
| 11. The store has no view of the other clusters, so several runs take one season (`01 case 11`) | FIXABLE | Genuinely improved: I6 evaluates the would-be component over the whole graph, so `nf:81091393-3` claimed by two Demon Slayer runs is refused on the second claim (proposal 5.5 I6, `01 R7`). The defect is the arbitration: "first accepted claim in order I7 wins", and I7 breaks ties "within a class by claim key", a sha-256 over the payload, which is content-addressed and therefore arbitrary between two runs of eleven episodes each ("which no count axis can separate", `01 R7`). Half the time the season lands on the wrong run, with a playable url, which is `01 I2`'s lie rather than its nuisance. The key also changes whenever any unrelated field in either payload changes, so the winner can flip between sessions. | when two candidates contradict and neither carries strictly better evidence (no date axis, equal counts), refuse BOTH and record the reason. Arbitrary arbitration is the one thing the exchange rate forbids |
| 12. A season that contains a run can lend it episodes without claiming to be it (`01 case 12`) | **FATAL** for the general class, HOLDS for the headline case | The proposal replaces `lendContainingSeason`'s re-pointing with a season row plus a `plugin:range` `INCLUDES` proved by shared air dates (proposal 4.4.1, 5.8). Episode release dates exist on the wire from **anizip, crunchyroll, appletv and the seed only** (`03 section 3` "Episode facts worth pulling out"), and anizip answers only when the cluster carries a mal id (`03 anizip` addressability). So the run side of the date axis is present only when anizip answered. Today the loan needs no date on the run side at all: `runEpisodes`' window keeps `1..length` of the lent season (`02 section 2.9` gate 4), which is why Mushoku season 1 cour 1 gets Crunchyroll's episodes 1 to 11 from a season of 23 with no alignment. Under the proposal a run with a length and no dated episodes of its own gets **zero** rows where today it gets the correct prefix. The record calls this path SHIPPED and works (`01 case 12`, 0 to 12 Crunchyroll sources live). | keep the count plus window loan as a second, lower-precision `INCLUDES`: when the container's numbering starts at 1 and the run's agreed length is backed by two declared witnesses, `fromEpisode 1..L` maps to `mapsFrom 1..L`, precision `COUNT_PREFIX`, retractable like any edge. It is exactly what ships today and it is not arithmetic placement inside the season |
| 13. A source carrying its numbering on from the previous season (`01 case 13`) | HOLDS | The requested primitive is delivered literally: `LINKS {kind:'ALIGNS'|'INCLUDES', fromEpisode:13, toEpisode:20, mapsFrom:1, mapsTo:8, score:8, evidence:{days,...}}` (proposal 3.2, 8.2), with the stored episode keeping Crunchyroll's number (`02 section 2.9` "READ TIME, AND A COPY"). `MIN_ALIGNED = 2`, the one day slack and the tie refusal are carried verbatim (proposal 5.8). | nothing, except that the demotion of the 8-of-12 Crunchyroll row is the case 10 problem above |
| 14. Three numbers per episode in three spaces (`01 case 14`) | FIXABLE | The display answer is right (slot number is the run's numbering, 1 to 12 for a split cour, proposal 5.9 step 4, the owner's call). But the case's "must express" is several numbering systems **each labelled with its space**, and no payload field says whether an `episodeNumber` is run-relative, season-relative or absolute. The design gets away with it because the only alignment axis it uses is dates; any future rule that reads a number without a date has nothing to read. | add `episodeNumberSpace: RUN | SEASON | ABSOLUTE` beside `startDatePrecision` in the additive schema set (proposal 4.5). anizip and crunchyroll can both set it today |
| 15. A mid-season insert makes the true mapping piecewise (`01 case 15`) | HOLDS | "Several edges between the same pair, one per contiguous segment, each with its own witnesses" and "a segment is never extended past what its witnesses cover" (proposal 3.2) is the right shape, and Blue Exorcist mints nothing because Netflix has no dates and `nf` is excluded from the title axis (proposal 8.3). | nothing |
| 16. Duplicate rows inflate the season length (`01 case 16`) | FIXABLE | C4 says it counts "distinct episode ids, not rows" (proposal 5.6), but identity I5, the rule that actually decides whether the season becomes a member, reads `json_extract(p.raw, 'episodeCount')` (proposal 5.5 candidate scan), which is `season.episodes.length` = 14 over 10 distinct epids. So a 10 episode season reports 14 against a 10 episode run and is demoted as a fold. That is a missing link rather than a lie, but the case is claimed as handled. | derive C once, as `count(DISTINCT e)` over `HAS_EPISODE` when the anchors exist, and use that single value in I5, C4 and the aggregation |
| 17. A streaming and a metadata catalogue disagree about the season NUMBER (`01 case 17`) | HOLDS | `providerContentId` never appends an ordinal (`01 case 17`), and the proposal changes nothing source-side. `similarMedia` remains the way to ask the owning origin (proposal 7.1). | nothing |
| 18. A source renumbering every season from 1 on one id (`01 case 18`) | HOLDS | Two independent barriers. The offenders are CONTAINER-scoped rows (`paramount` always, `unogs` bare title id, `trakt`, `tvdb`, `omdb` for a series: `03 section 1`), so I4 never admits them as members; and proposal 3.4 forbids walking `HAS_EPISODE` across a `PART_OF` at all, which is `sameAsHandleUris`' guard made structural rather than a filter one caller can forget (`02 section 4.1`). | nothing |
| 19. The same episode from two sources drawing two lists (`01 case 19`) | HOLDS | `EpisodeSlot` keyed `(clusterId, number)` with the mapped number (proposal 5.9 step 4), scoped to one cluster and its proven ranges, which is `01 I14`'s precondition written into the key rather than into a comment. Specials keep their own slot keyed by uri. | nothing |
| 20. A field-by-field merge producing a row no source published (`01 case 20`) | HOLDS | The hybrid row is still constructible (fields are still first non-null in score order, proposal 5.9), but `Aggregate.provenance` and `EpisodeSlot.provenance` name the payload and origin per field, which is exactly the case's "must express", and the contamination that caused it is closed by the slot rules. | nothing |
| 21. Placeholder episode titles (`01 case 21`) | HOLDS | `GENERIC_EPISODE` exclusion carried into the range plugin's TITLE axis (proposal 5.8), counting real titles rather than rows. | nothing |
| 22. An episode count that counted something else (`01 case 22`) | FIXABLE | The proposal relies on a declared/derived split in three separate rules (I5's L, 5.9's `episodeCount`, the loan rule's two declared witnesses) and supplies it as a hard-coded origin list inside `plugin:aggregate` ("DECLARED counts first (mal, anilist, anizip, offline, jw `totalEpisodeCount`)"). That list is wrong for at least two of its members: kitsu's `episodeCount` is `attr.episodeCount` **overwritten by the fetched list length** (`03 kitsu`), and justwatch prefers the released count over `totalEpisodeCount` (`03 justwatch`), so both are declared on one path and derived on another and no per-origin constant can tell them apart. This is `01 case 44`'s mistake in a new place: a per-source constant standing in for a per-claim property. | `countKind: DECLARED | FETCHED_LIST | RELEASE_LIST` as a fifth additive schema field (proposal 4.5). It is the field `01 case 22` and `01 R3`'s conclusion both asked for |
| 23. "No count" read as zero, and zero vetoes everything (`01 case 23`) | **FATAL** as claimed, FIXABLE in fact | The proposal cites case 23 twice (P5, I5) as handled. It is not touched. The defect is in the extractors: `aggregateMedia` always emits `episodes: []`, so `?? media?.episodes?.length` at `appletv:233`, `unogs:351/361`, `justwatch:461/584`, `tvmaze:171` and `tmdb:161` reads 0, and `foldVetoed({episodeCount: 0}, candidate)` refuses every season silently. Proposal 5.9 keeps `episodes` off the aggregate (they come from `EpisodeSlot` through the field resolver), so those seven call sites see the same empty array and the same zero. Nothing in the plugin layer can see a veto that fires inside a source. | it is a source-side fix (read `episodeCount` only, never `episodes.length`), and it must be listed as one in the migration (proposal 10) rather than claimed by the core |
| 24. Normalising a title to `[a-z0-9]` deletes a CJK title (`01 case 24`) | HOLDS | `stripTitle` keeping `[\p{L}\p{N}\s]`, `carriesIdentity`'s HAS_LETTER filter, and the scorer dividing by `[...text].length` are all carried (proposal 5.7 profile, normaliser row). | nothing |
| 25. No threshold separates punctuation from a season number (`01 case 25`) | HOLDS | `differOnlyByTrailingNumber` compares the trailing number as a VALUE and skips the pair, and the season gate stays a gate (proposal 5.7 gates 2 and 5). | nothing |
| 26. Silence on one side, so a disagreement veto never fires (`01 case 26`) | HOLDS | Silence never blocks, the `86` / `86 Part 2` weld stays as a named accepted failure (proposal 5.7 failure mode), and `01 R4`'s one-stopped-per-37-destroyed is cited as the reason. | nothing |
| 27. A franchise title collapsing a sequel onto its parent exactly (`01 case 27`) | HOLDS | The two-axis catalogue gate lives in `catalogue-gate.ts` and is untouched (proposal 9.1). The store-side title pass keeps its year bucket and 45 day window as its second axis. | nothing |
| 28. The diacritic gate (`01 case 28`) | HOLDS | `foldDiacritics: false` by default, with the measured change named as a prerequisite (proposal 5.7 failure mode, 11.4). | nothing |
| 29. Synonym fields carrying programme names (`01 case 29`) | HOLDS | Not on the wire and not read (proposal 5.7 evidence). The payload-verbatim rule does not reintroduce them because no extractor emits them. | nothing |
| 30. The six-title cluster profile, and which six (`01 case 30`) | HOLDS | Title ascending within a tier, cap 6, with the three measured arms cited (proposal 5.7 titles row). Deduplication per normalized title across payloads keeps the cap meaningful now that one origin can have several payloads. | nothing |
| 31. Intra-word punctuation splits an index term (`01 case 31`) | FIXABLE | Not expressed. The case asks for derived spelling variants as first-class alternates carrying the rule that derived them; the proposal's only derived title is `Aggregate.searchKey` (`stripTitle` of every title, proposal 5.9). The impact is bounded because this is the nyaa plugin's own search domain, which reads the aggregate rather than the graph. | if it matters, a `TitleVariant` node stamped by the plugin that derived it, joined to the anchor; it is exactly the shape the plugin tables already have |
| 32. Normalising a match score by the wrong side (`01 case 32`) | HOLDS | The normaliser is applied to both sides of every comparison (proposal 5.7 normaliser row), and episode titles are compared by exact equality after `stripTitle` on both sides, never by a threshold (proposal 5.8 TITLE row). | nothing |
| 33. `YYYY-01-01` is a sentinel and a real premiere at once (`01 case 33`) | HOLDS | `years` buckets on the sentinel, `days` drops every first of a month, and the optional `startDatePrecision` lets a source opt out without touching the others (proposal 5.7 days row, 4.5). The 77% of streaming attaches that depend on the guard are safe because the default reading is unchanged. | nothing |
| 34. A month-precise date that reads as day-precise (`01 case 34`) | HOLDS | Same mechanism; kitsu and jikan are named as the first opt-in candidates (proposal 11.7). | nothing |
| 35. A source dating a media by the NEXT airing (`01 case 35`) | HOLDS | Source-side, `aired-date.ts`, untouched. The derivation axis the case asks for is available as the optional precision field. | nothing |
| 36. Cross-source date agreement is excellent, but only on TV (`01 case 36`) | HOLDS, with a gap | The 45 day window and its structural ceiling (two cours sit about 91 days apart) are carried (proposal 5.7 gate 3). The case's second half, that the agreement rate depends on FORMAT (TV 0 of 207 outside 45 days, SPECIAL 5 of 102), is not expressed: one window for every format, as today. No regression. | a per-format window is a measured change, not a free one; leave it, but stop citing the case as fully expressed |
| 37. The date is the key and an ordinal would not be (`01 case 37`) | HOLDS | `similarMedia` and its five answerers are kept whole (proposal 9.1), and the answer now carries its own `relation` so an imprecise answer says so (proposal 4.4.2). | nothing |
| 38. A date naming a DAY versus an INSTANT (`01 case 38`) | HOLDS | The range plugin compares UTC days with one day of slack (proposal 5.8 DATE row), and `releaseDatePrecision` is available for the render path. | nothing |
| 39. Two catalogues numbering different things in one namespace (`01 case 39`) | FIXABLE | The anchor is keyed on the uri string alone (proposal 2.1), so `tmdb:550` is one node for Fight Club and for Till Death Us Do Part. The case's "must express" is an id namespaced by (origin, KIND). Today the protection is source-side refusals (simkl, watchmode) and `trakt/extractor.ts:79` is still open (`01 part 4 problem 17`), so the open hole is inherited. | either keep the refusal on the claimant (status quo, and say so) or make the anchor key `(origin, kind, id)` and let a claim that cannot name a kind land on a third anchor |
| 40. A positional path read that mints a constant (`01 case 40`) | FIXABLE | Blast radius is contained rather than detected: every record minting `anidb:animedb.pl` claims one anchor, the closure would merge all of them, and I6 refuses every claim after the first because the component holds many mal ids. So one run keeps a wrong link and the rest are refused, which beats today's weld. But the case's "must express" is that the minting RULE travels with the id so a constant is detectable the moment a second record produces it, and no such property exists. Note one accidental protection worth keeping: `hbo:watch` and friends have no describing payload, so `01 case 40`'s 25-title HBO cluster cannot form. | an anomaly plugin over `CLAIMS`: an anchor of origin X claimed by N payloads whose own components disagree is a constant-valued id. It is one `count(DISTINCT)` query and it needs no expected answer, which is `01 case 46`'s whole argument |
| 41. A container id that is a film collection (`01 case 41`) | FIXABLE | The PART_OF half holds (fifteen kitsu films point at `cr:GQWH0M1GG`, no membership). The danger is C4: fifteen film runs of length 1 ordered by start day against a collection whose fetched list is 15 is a unique contiguous window, so C4 would mint fifteen `INCLUDES` edges placing film i at position i, which is placement by order, the `R1`/`R20` family. | C4 must refuse when every candidate run has length 1, or better, be deleted (case 9) |
| 42. Show-level ids with no finer alternative (`01 case 42`) | HOLDS | `Aggregate.handles` renders a PART_OF target's url from `CLAIMS.node` when the target has no payload (proposal 4.2, 5.9 handles row), and registering `imdb` as an origin is migration step 7. That closes `01 part 4 problem 11`. | nothing, but see case 48 for the SAME_AS half of the same mechanism, which is missing |
| 43. Per-run ids that are show-level for a minority (`01 case 43`) | FIXABLE, and priced wrongly | `mal:51535` is claimed by both AoT Final Chapters parts. The closure merges them, I6 sees two anilist ids, and the second claim is refused: one part keeps MAL, the other loses the entire 0.9 tier (titles, description, score, popularity, genres, `episodeCount`), and which part loses is decided by a content hash (I7 tie-break). `mal` is per-run for ~99.7%, so this fires on the 0.3% that are not, and the record priced the blunt version of this at 3,184 correct merges lost. The proposal's version is narrower, but its exchange rate is unmeasured and it is presented as a straight promotion of a test-only rule to a production veto. | measure the arm before shipping I6 as a veto: how many components does it split, and what does each loser lose. Until then, report rather than refuse (which is what `02 section 2.11` deliberately does) or refuse symmetrically (case 11) |
| 44. Scores are per-source constants deciding every scalar (`01 case 44`) | FIXABLE | Per-field provenance is delivered, so a wrong field is now visible and attributable. The case's actual "must express", a per-field per-source confidence rather than one constant doubling as quality, identity and tiebreak, is not: `Payload.score` is the row's constant and the aggregation still sorts on it (proposal 5.9). anizip's missing row score is handled by an off-by-default constant (11.5), which is the same shape the case complains about. | the scored arrays already carry per-entry scores; extend that to the scalars a source can rank itself on, or state plainly that the constant stays and drop the claim on case 44 |
| 45. The count rule is lexicographic tiers, and a sum is wrong (`01 case 45`) | **FATAL**, one half | The tier rule survives (proposal 5.9 `episodeCount`, no sum anywhere). The trimming half does not. `consensus.ts:210-211` spares two sets from the window, `reference` (origins agreeing on the length) and `equals` (origins at or above the deciding tier), and the record's rule is "ONLY A STRICTLY LOWER TIER IS TRIMMED ... two 0.9 catalogues disagreeing is a disagreement, and the answer to one of those is to show what the tier's majority says, never to delete the dissenter's episodes". Proposal 5.9 step 3 is a bare "window `1..length` when a length exists with two witnesses", applied to every episode in the slot list. So with `anilist(0.8)=13 mal(0.9)=12`, AniList's thirteenth episode is deleted from the page, which is precisely what `01 I9`'s twice-the-weight margin exists to prevent. | restore the spared sets: a slot whose only members are origins in `reference` or `equals` is never windowed away. It is four lines of the function the proposal already keeps (`runEpisodes`) |
| 46. The contradiction rules (`01 case 46`) | FIXABLE | `disagreeingIds` gets a production caller as I6 (see case 43 for the cost question). `overLength` becomes "an assertion in tests and a `log` event" (proposal 9.2), so the one rule the membership rules structurally cannot see still has no runtime consumer. The case's "must express" (rules evaluable over the whole graph at once, with the evidence they read present) is otherwise delivered: counts, per-origin ids and the listed set are all queryable. | make anomalies a plugin producing a table. It writes no identity, so it cannot hurt, and it is the natural home for case 40's constant-id detector |
| 47. A seed of the store's own output fed back in (`01 case 47`) | HOLDS, and improves | `provenance:'SEED'` never stamps a scope (I1), and `exportGraph` walks `CLAIMS` with `provenance = 'SOURCE'` only (proposal 9.2), which closes "seed N+1 ratified seed N" structurally rather than through `?seed=off`. The re-broadcast path that carried it (justwatch's `mergeHandles` over `buildHandlesFromUri`) is `URI`-stamped and therefore also excluded. The second finding (the seed opening another source's evidence gate with six titles and a startDate) depends on the seed staying thin: the title profile reads every payload's titles and does not filter on origin. | let the title profile weight or exclude by `Payload.origin` so a fat seed cannot evict live titles from the six slots again |
| 48. A claim that arrives before the row it is about (`01 case 48`) | **FATAL**, by a side effect | The deferral itself is elegant: the anchor exists, the claim sits on it, I2 returns `DEFERRED`, no queue (proposal 2.1, 5.5 I2). The fatal part is the interaction with proposal 4.2 ("a nested handle node never becomes a row") and 5.5 ("`MEMBER_OF` for every anchor that has at least one payload"). Today a JustWatch offer node is `makeMedia({origin, id, url})` (`justwatch/extractor.ts:399`, confirmed in the tree), and `url` is not an identity field, so `isPlaceholder` is false (`db.ts:104-108`, confirmed), the node IS stored, and the film's SAME_AS offer handle becomes a cluster member whose badge the UI renders. Under the proposal that anchor has no payload, so it can never be a member, so it never appears in `Aggregate.handles` (members only). Six origins exist for no other purpose than rendering that badge (`03`: disney, amazon, hulu, peacock, hbo, fubo "supply nothing ... they exist so JustWatch has an origin to mint a handle into and so a row can render with a name and an icon"). The proposal patches the PART_OF half of this (case 42) and not the SAME_AS half, so every film-level provider link and every watchmode provider handle on a film silently disappears. `01 case 40` measured that class at 515 handles and 119 offers. | render an accepted SAME_AS claim to a payload-less anchor as a handle from `CLAIMS.node`, exactly as the PART_OF rule already does, while keeping it out of `MEMBER_OF` so it contributes no field. That is the honest reading of the placeholder rule: identity only, no field, still rendered |
| 49. A container cluster with no run on the page (`01 case 49`) | FIXABLE | The good half: a lent or borrowed season is a first-class cluster with its own page (proposal 6.4), which closes the orphan the record flagged as a blocker. The changed half: `Aggregate.attached` is a GLOBAL property of the cluster, whereas `hideAttachedContainers` hides a container only when a run cluster **in the same list** points at it (`02 section 1`). So a container whose runs are filtered off the current page is now hidden everywhere instead of getting its card. | compute `attached` as a property and apply it against the page's own cluster set in 6.1, which is one extra `EXISTS` in the query |
| 50. A relation is a different axis from sameness (`01 case 50`) | HOLDS | Narrative relations stay inside `Payload.raw` and are forwarded, never traversed (proposal 3.1), and the closed `kind` set fails at compile time, which is `01 R16`'s reason. | nothing |
| 51. Episode containment declared, written and read by nothing (`01 case 51`) | HOLDS | `EPISODE_PART_OF` is retired in favour of `INCLUDES` with a range, which has a writer (`plugin:range`) and a reader (`EpisodeSlot` step 2). The unguarded `graph.link(episodeUri, handleUri, EPISODE_SAME_AS)` disappears with the union-find. | nothing |

---

## Part 2. The 26 refutations (does any plugin re-commit one?)

| refutation | verdict | how |
| --- | --- | --- |
| R1. A single offset aligning a Netflix season by title vote | HOLDS | No offset is ever voted from titles. Segments need witnesses and stop where the witnesses stop (proposal 3.2); Blue Exorcist mints nothing (8.3) |
| R2. A per-episode title join minting EPISODE_SAME_AS | HOLDS | `nf` excluded outright from the TITLE axis, exact equality only, three matches and 0.6 coverage, no similarity threshold on an episode title, ever (proposal 5.8). R2's surviving half (titles between two metadata catalogues) is what remains |
| R3. A weighted sum of source scores for `episodeCount` | HOLDS | Lexicographic tiers, explicitly "never a sum" (proposal 5.9). The declared-before-derived ordering is R3's own stated conclusion, though it is implemented as a per-origin constant (case 22) |
| R4. Making season SILENCE block a merge | HOLDS | "Silence never blocks" with the 323-for-12,007 figure cited (proposal 5.7 gate 2) |
| R5. A quarter check as the date comparison | HOLDS | Day window at 45, no quarter anywhere |
| R6. Tuning `SIMILARITY_THRESHOLD` | HOLDS | 0.9 verbatim, with "change nothing" cited (proposal 5.7 gate 5) |
| R7. unOGS ordinal and count agreeing | HOLDS, with a caveat | Not repeated. The welds it could not stop are caught by I6 instead, but by arbitrary arbitration (case 11) |
| R8. Refusing JustWatch's `nf` handles | HOLDS | Not repeated; `nf` handles land as claims and are judged on scope |
| R9, R10. kitsu film links: blanket refusal, denylist | HOLDS | The (origin, segment) allowlist stays source-side, untouched |
| R11. Dropping the kitsu subtype gate | HOLDS | Untouched |
| R12. A lone-season carve-out for JustWatch's Crunchyroll refusal | HOLDS | Untouched |
| R13. Apple TV's gate without season-scoped ids | HOLDS | Not rewired; Apple TV's scope demotion stands |
| R14. Raising the `offline` or `kitsu` SCORE | HOLDS | `DEFAULT_ROW_SCORE` covers anizip only and is off by default (proposal 11.5) |
| R15. Capping each source at one re-ask | HOLDS | `MAX_ASKS_PER_PAIR = 4` per pair, re-ask on a changed question (proposal 7.2) |
| R16. A second `links` field instead of an edge type | HOLDS | A closed `kind` set on one rel table (proposal 3.1) |
| R17. Widening only `normalizeTitle` | HOLDS | The normaliser is applied to both sides and to both the profile and the scorer (proposal 5.7) |
| R18. Reading `extendsId` symmetrically | HOLDS | One way, cited (proposal 5.5 I6) |
| R19. The three public mapping datasets for Netflix episodes | HOLDS | Not used |
| R20. Composing a tvdb `episodeoffset` onto a Netflix season | **RE-COMMITTED** in C4 | C4 computes `INCLUDES` ranges "by cumulative offset" from counts alone (proposal 5.6), which is offset composition with a different lookup table, and proposal 6.4 renders those ranges to the user as "holds: season 1 part 1, 1..11". The mitigation offered is that a `COUNT` range lends no episodes, but the case 9 arithmetic shows the edge itself can be wrong, and a wrong edge is rendered as a `PART_OF` handle with a playable url |
| R21. uNoGS `imdbid` as a dedup handle | HOLDS | Not re-added |
| R22. The ani.zip KEY in the release-number map | HOLDS | Plugin-side, untouched |
| R23. sacha as the release parser | HOLDS | Untouched |
| R24. Dropping nyaa's trusted pass | HOLDS | Untouched |
| R25. The per-episode gap query on nyaa | HOLDS | Untouched |
| R26. A translation bridge for ani.zip numbering inside stub | HOLDS, and improves | R26 was refuted because `Media.episodes` regrouped from scratch in the resolver, so a rule in `mergeByEpisodeNumber` shipped inert. `EpisodeSlot` is the single grouping point and the resolver reads it directly (proposal 6.2), so a rule now lands where it is read |

---

## Part 3. The 15 invariants

| invariant | verdict | how, or why not |
| --- | --- | --- |
| I1. Irreversibility | HOLDS | No union-find survives a pass; membership is recomputed from claims and `reconcile` deletes by natural key (proposal 5.1, P3). The one residue: `Cluster.aliases` only ever grows, and a split "keeps the id on the larger part", so an id absorbed before a split keeps resolving to the surviving cluster, which is a stale answer to an old `_id` |
| I2. The RUN versus SHOW exchange rate | FIXABLE | The refusal side is respected everywhere. The counterweight ("refusing is not free", measured three times) is not: I5's `C < L` demotion (case 10), I6 as a production veto (case 43) and the global `attached` (case 49) are all new refusals with no measured arm, and P7 requires exactly that. `01 I2`'s own examples (66 handles per 13 suspect, 37 merges per weld, 77% of streaming attaches) are the model that these three do not follow |
| I3. Scope is a ratchet toward CONTAINER | FIXABLE | Computed rather than stored and therefore un-ratchetable, which is an improvement. But I1's effective-scope order lets any `SOURCE` claimant's `targetScope` stamp outrank the anchor's own describing payload ("CONTAINER if any of the first three says so"). Today a claimant's stamp only matters when its node is stored as a non-placeholder, and `01 case 47`'s third finding is the measured precedent for why a foreign stamp must not overwrite a live row |
| I4. An id must be reproducible by a second observer | HOLDS | The core mints no id; `Cluster.id` is internal and `ag:(...)` is built from member uris |
| I5. Provenance loss, four shapes | HOLDS, mostly | (1) and (3) closed by keeping payloads, (2) closed by `CLAIMS` plus `VERDICT`, (4) closed by counting payloads that restate one nested node. Residue: within one origin a scalar is "the latest payload that carries it" (proposal 5.9), and `seq` is the core's write counter, i.e. HTTP arrival order, so which of a search row and a media row supplies a field still depends on arrival. Same as today; but it is claimed as deterministic under I15 |
| I6. Folded cours | FATAL, through C4 | The one-directional fold veto and the lend-without-identity survive. C4 adds a second, count-only containment rule whose worked example is arithmetically wrong (case 9) |
| I7. Retranslated titles | HOLDS | `nf` excluded, exact equality only, no threshold |
| I8. The diacritic gate | HOLDS | Off until measured |
| I9. The 13 versus 12 run length | FIXABLE, two ways | The loan rule's two declared witnesses are carried. But (a) the window's spared sets are dropped, so the thirteenth episode AniList may be right about is deleted rather than kept (case 45), and (b) I5's fold veto requires "an agreed length backed by two declared counts", so on exactly this cluster (MAL publishes no count) the veto is inert and any folding-catalogue SAME_AS claim is "accepted as claimed". The proposal presents I5 as what closes cases 9, 10 and 12 |
| I10. The 24 versus 2x12 fold | FIXABLE | Slots are built from members and proven ranges only, which closes the structural cause. The two ways 24 rows could come back: a folding season admitted as a member because I5's premise is missing (I9 above), and a wrong C4 edge (case 9) |
| I11. The tier rule | FIXABLE | The value rule survives; the trim margin (two witnesses AND a strictly lower tier AND twice the weight) does not (case 45) |
| I12. Main titles only | HOLDS | Synonyms are not on the wire and the profile reads `titles` |
| I13. The season pair | HOLDS | Quoted as a pair from one payload (proposal 5.9) |
| I14. Grouping by number inside one cluster | HOLDS | The slot key is `(clusterId, number)` and the input is members plus proven ranges |
| I15. Determinism | FIXABLE | Better than today on the big axis (no arrival-order union, no arrival-order six titles). Two new order dependencies: I7 breaks ties "within a class by claim key", a content hash, so which of two contradicting claims wins is arbitrary and flips whenever any unrelated field in either payload changes (case 11, case 43); and "latest payload wins" inside one origin is arrival order (I5 above) |

---

## Summary

### FATAL (cannot be satisfied without changing a principle or a worked example)

1. **C4, the count-exact fold, is incoherent as written and unsafe under its only working reading (case 9, `01 I6`, `01 I10`).** As specified it selects "a container cluster member that is a RUN-scoped season row", a set identity I4 guarantees is empty. Read charitably (a RUN-scoped season row `PART_OF` the same container), the proposal's own walkthrough is wrong: with cours 11, 12, 12, 12, 14 the contiguous pair sums are 23, 24, 24, 26, so JustWatch's season of 24 matches two windows and C4's "exactly one such window" must refuse the edge 8.1 claims it mints, while any relaxation that admits it also admits Netflix season 1 = 24 pointing at cours 2 and 3, rendered as a `PART_OF` handle with a playable Netflix url on two wrong run pages. Only the sums 23 and 26 are unique here, so the rule is inert where it is safe and wrong where it is useful. It is `R20`'s offset composition with counts as the lookup table.
2. **An accepted SAME_AS claim to an anchor with no payload disappears from the page (case 48, case 42, case 40).** `MEMBER_OF` is emitted only for anchors with a payload (5.5) and handle nodes never become payloads (4.2), while `Aggregate.handles` builds SAME_AS from members. Six origins exist solely to render such a handle (`03`: disney, amazon, hulu, peacock, hbo, fubo), and JustWatch's offer node carries a `url`, which today makes it a stored non-placeholder and therefore a member (`db.ts:104-108`, `justwatch/extractor.ts:399`, both confirmed in the tree). The PART_OF half of this is patched and the SAME_AS half is not, so film-level provider links and watchmode's provider handles vanish. `01 case 40` measured that class at 515 handles and 119 offers.
3. **The loan now requires dated episodes on the RUN side, which most runs do not have (case 12).** `plugin:range` is the only path by which a containing season reaches a run, and episode release dates are on the wire from anizip, crunchyroll, appletv and the seed only (`03 section 3`), with anizip answering only when the cluster carries a mal id. Today `runEpisodes`' `1..length` window lends the correct prefix with no date at all, which is how Mushoku season 1 cour 1 gets Crunchyroll's episodes 1 to 11 from a season of 23. The proposal deletes that path and calls it arithmetic placement, which it is not: the placement is the container's own numbering, trimmed by the run's own corroborated length.
4. **The episode window drops the spared sets, so an episode that aired is deleted (case 45, `01 I9`, `01 I11`).** `consensus.ts:210-211` spares `reference` and `equals` origins from the window and trims only a strictly lower tier; proposal 5.9 step 3 windows everything to `1..length`. With `anilist(0.8)=13` against `mal(0.9)=12` the thirteenth row goes, which is the exact outcome the twice-the-weight margin was measured to prevent.
5. **Case 23 is claimed and not delivered.** The zero-for-absent defect lives in seven source call sites reading `media.episodes.length` off an aggregate that always emits `[]`, and proposal 5.9 keeps `episodes` off the aggregate. No plugin can see a veto that fires inside a source.

### SERIOUS (wrong or missing, fixable inside the design)

- **I5's fold veto is inert on the clusters with the thinnest evidence** (`01 I9`): it needs "an agreed length backed by two declared counts", and Mushoku season 2 part 1 has one. The design leans on I5 for cases 9, 10 and 12.
- **I5's `C < L` demotion fires on every airing show** (case 10): a Crunchyroll row whose fetched list is short loses membership, and with it its cover, banner and description (0.5, above kitsu), for a whole broadcast season. The declared/derived distinction the rule needs is not on the wire (case 22).
- **I6 is promoted from a test-only report to a production veto with no measured arm, and arbitrates arbitrarily** (cases 11, 43, `01 R7`). Two runs of eleven episodes each contradicting over one Netflix season is settled by a sha-256 tie-break, so half the time the playable link lands on the wrong run; `mal:51535` on two AoT parts strips one part of the entire 0.9 tier, with the loser chosen by hash. `01 I2` says a wrong row is a lie, and an arbitrary winner mints one.
- **The declared/derived count class is a per-origin constant inside a plugin** (case 22), and it is wrong for kitsu (`attr.episodeCount` overwritten by the fetched list) and for justwatch (released count preferred over `totalEpisodeCount`), which is `01 case 44`'s mistake repeated.
- **A foreign `SOURCE` stamp outranks the anchor's own describing payload** in effective scope (I3), widening a rule the record narrowed after the seed overwrote a live row (`01 case 47`).
- **`attached` is global where `hideAttachedContainers` is per page** (case 49), so a container whose runs are filtered off the page loses its card.
- **A read now answers only after a completed pass** (proposal 6.2, 11.1). `waitForMedia` polls from inside a source resolver while the runner serialises every plugin, and case 12's "four defects only the running app showed" are all timing defects of exactly this shape. The proposal's mitigation ("the deadlines are unchanged") is not an argument about the new latency.

### MINOR

- Episode number spaces are not labelled (case 14); only the date axis saves it.
- `LINKS` granularity is mixed: 2.2 declares `FROM Media TO Media`, 3.2 and 8.1 mint media-level `INCLUDES`, 5.6 mints cluster-level, and 5.9's aggregate query collects only cluster-level `INCLUDES`, so lent episodes may not reach the slot at all as written.
- `VERDICT` is `FROM Payload TO Media`, but the identity scan also judges `LINKS` rows from `plugin:title`, which have no payload endpoint, so those verdicts cannot be written.
- I5 reads the inflated `episodeCount` while C4 reads distinct ids (case 16).
- `Cluster.aliases` is never retracted, so an old `_id` can resolve to the wrong cluster after a split (I1).
- The year set is not attributable to the payload that supplied it (case 3).
- No per-format date window (case 36); no title variant table (case 31); no `(origin, kind)` anchor key (case 39); `overLength` still has no runtime consumer (case 46).

### HOLDS (the hardest cases it clearly handles)

- **One chokepoint** (case 7): `MEMBER_OF` has exactly one producer and the runner enforces it.
- **The same evaluation path for an id claim and a title guess** (case 2): `plugin:title` emits `LINKS SAME_AS` and identity judges it with I4, I5 and I6, so a guard written once applies to both, and it is reachable from the direction the claim arrives in.
- **Nothing reached through `PART_OF` or `INCLUDES` is ever treated as `SAME_AS`** (proposal 3.4, cases 18, 51, `01 I10`): the filtered path cannot step onto another kind, and the episode walk is structurally forbidden from crossing a container, which retires `sameAsHandleUris`' "most dangerous read in the tree".
- **Reversibility** (`01 I1`): membership is recomputed per pass and every plugin row is retractable by its stamp.
- **The range form is the primitive the record asked for** (cases 13, 15, 51): explicit `fromEpisode/toEpisode/mapsFrom/mapsTo` with witnesses, piecewise segments, and no arithmetic past the last witness. Blue Exorcist mints nothing (`01 R1`), the Elusive Samurai's 13..20 maps to 1..8 (`01 case 13`).
- **The seed can no longer ratify itself** (case 47): `SEED` never stamps and the export walks `SOURCE` claims only.
- **The address-bar handle is a pointer** (case 8, `01 part 4 problem 2`).
- **Title matching is carried verbatim**: 0.9, the trailing-number value comparison, the six-title cap in title-ascending order, the both-sides normaliser, the day1 guard, the 45 day window, the companion veto's two signals (cases 24, 25, 26, 30, 33, 34, 36, `01 R4`, `01 R6`, `01 R17`).
- **No refuted approach is retried** except `R20` inside C4: no offset vote, no weighted sum, no silence veto, no threshold tuning, no Netflix title join, no mapping datasets.

**Score: 6 / 10.** The spine (one chokepoint, retractable verdicts, provenance on every claim, a closed edge set, ranges with witnesses, the traversal prohibition) is the right answer to the brief and to `01 I1`. The failures are concentrated in four places: one new rule that is either inert or mints a wrong playable link (C4), one omission that silently deletes a shipped feature (payload-less SAME_AS handles), one replacement that drops evidence today's code keeps (the count plus window loan, and the window's spared sets), and a set of new refusals whose exchange rates were never measured, which is the one habit `01 I2` says every rule in this repo has had to earn.
